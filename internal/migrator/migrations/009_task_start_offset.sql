-- Целочисленное смещение (lead/lag) планового старта ролевой задачи
-- относительно окончания предыдущей ролевой группы внутри той же стори
-- (аналог lead/lag на FS-зависимости в MS Project). Может быть отрицательным
-- (начать раньше) или положительным (намеренная задержка). Дефолт 0 —
-- поведение планировщика не меняется для уже существующих задач.
ALTER TABLE gantt_tasks ADD COLUMN IF NOT EXISTS start_offset_days INT NOT NULL DEFAULT 0;
