## 1. Backend — настройка команды

- [x] 1.1 Создать миграцию под настройку команды «не занимать промежутки в прошлом»: булево поле со значением по умолчанию «выключено», чтобы после применения граница поиска совпадала с прежней (design.md Решение 3, Migration Plan). Проверка: миграция применяется на чистой БД и на копии существующей, у всех команд настройка выключена.
- [x] 1.2 Добавить чтение настройки в доменную модель и репозиторий, а также её запись. Проверка: `go build ./...` проходит, тесты репозитория на чтение и запись значения проходят.
- [x] 1.3 Читать настройку в начале `RecalculateTeamSchedule` — пачкой на команду, вне цикла по задачам, там же, где уже читаются пулы исполнителей и закрепления (design.md Решение 3). Проверка: тест — пересчёт, вызванный любым способом, видит актуальное значение настройки.

## 2. Backend — интервальный календарь

- [x] 2.1 Заменить `assigneeFreeAt map[uuid.UUID]time.Time` на календарь занятых интервалов (`internal/gantt/ganttService.go`, `scheduleState`), сохранив пересоздание на каждый вызов `RecalculateTeamSchedule`. `advanceFreeAt` превращается в резервирование интервала (design.md Решение 1). Проверка: существующие тесты `internal/gantt/` проходят без изменения ожиданий по датам там, где промежутков нет.
- [x] 2.2 Реализовать поиск самого раннего подходящего промежутка (design.md Решение 2): минимальное `t ≥ max(target, floor)`, при котором `[t, t+d]` целиком лежит в свободном интервале; при отсутствии такого `t` — размещение после последней брони. Проверка: тест — задача, целиком помещающаяся в простой перед более приоритетной, занимает его; задача, которая не помещается, встаёт после последней брони и не дробится.
- [x] 2.3 Перевести выбор исполнителя на новую функцию получаемой даты старта: тот же жадный `argmin start(u)` с round-robin тай-брейком, но `start(u)` теперь ищет промежуток. Политику выбора не менять. Проверка: существующие тесты на параллельное распределение, round-robin и приоритет закрепления проходят без изменения ожиданий.
- [x] 2.4 Вычислять границу `floor` из настройки команды: выключена — `teamFloor`, включена — `max(teamFloor, today)` (design.md Решение 3). Граница действует на всё размещение (`earliest = max(target, floor)`), а не только на поиск промежутка: при включённой настройке раньше текущей даты не планируется ни одна задача. Проверка: тест — при включённой настройке промежуток, начинающийся раньше текущей даты, не занимается; при выключенной занимается.
- [x] 2.5 Резервировать для замороженных задач (`Progress > 0` или `ActualEndDate != nil`) их фактический интервал вместо сдвига ватерлинии (design.md Решение 4). Исполнитель замороженной задачи по-прежнему не переназначается. Проверка: тест — замороженная задача занимает в календаре свой интервал, промежуток перед ней доступен при выключенной настройке и недоступен при включённой.
- [x] 2.6 Распространить интервальную модель и поиск промежутка на календарь роли без исполнителей (`roleFreeAt`, design.md Решение 5). Проверка: тест — задача роли без носителей занимает промежуток, задачи этой роли между собой не пересекаются.

## 3. Backend — HTTP API

- [x] 3.1 Отдавать значение настройки команды наружу, чтобы интерфейс мог показать текущее состояние переключателя. Проверка: тест обработчика показывает актуальное значение.
- [x] 3.2 Реализовать обработчик переключения настройки с team-scoped проверкой администратора команды — тем же приёмом, что уже применяют `admin.go:372` и `admin_scores.go:81`; роль `member` получает отказ. Ошибки — в стандартном формате `{ "error": { "code": "...", "message": "..." } }`. После переключения расписание команды пересчитывается. Проверка: тесты обработчика на успешное переключение, отказ для `member`, отказ для админа чужой команды.
- [x] 3.3 Зарегистрировать маршрут в существующей аутентифицированной группе и добавить новые методы в контракты обработчиков. Проверка: `routers_mount_test.go` подтверждает, что маршрут смонтирован и закрыт middleware авторизации.

## 4. UX — постановка для фронтенда

- [x] 4.1 Провести UX-ревью и сформировать постановку (субагент `ux`, код не пишет): где на вкладке «Гант» разместить переключатель, чтобы он читался как настройка команды, а не как фильтр отображения; как назвать его так, чтобы смысл был понятен без объяснения алгоритма; нужно ли пояснение о том, что переключение меняет расписание, и как показать, что пересчёт произошёл; состояние для пользователя без права редактирования. Проверка: постановка зафиксирована текстом с перечнем состояний и критериями приёмки, согласована с существующей дизайн-системой `web/gantt/css/`. Постановка — `ux-brief.md` в каталоге заявки.

## 5. Frontend

- [x] 5.1 Добавить переключатель настройки на вкладку «Гант» по постановке из 4.1, с загрузкой текущего значения и вызовом сохранения. Проверка: переключение меняет значение, диаграмма перерисовывается пересчитанными датами.
- [x] 5.2 Скрывать или блокировать управление настройкой для роли `member` тем же признаком, что и остальное редактирование. Проверка: под пользователем с ролью `member` переключатель недоступен.
- [x] 5.3 Реализовать остальные находки UX-постановки из 4.1, относящиеся к подаче. Проверка: соответствует критериям приёмки постановки.

## 6. QA

- [x] 6.1 Тест ключевого сценария: у более приоритетной стори длинный анализ, исполнитель поздней роли простаивает, задача менее приоритетной стори целиком помещается в простой. Проверка: задача занимает простой, даты более приоритетной задачи и всего, что от неё зависит, не изменились.
- [x] 6.2 Тест непомещающейся задачи и отсутствия дробления. Проверка: задача длиннее промежутка встаёт после последней брони целиком.
- [x] 6.3 Тест непересечения по каждому исполнителю на наборе, где часть задач заняла промежутки. Отдельный тест, а не проверка ожидаемых дат: параллельные бары одной роли теперь штатны, и пересечение задач одного человека на диаграмме легко не заметить (design.md, Risks). Проверка: ни одна пара задач одного исполнителя не пересекается.
- [x] 6.4 Тест соблюдения предусловий: промежуток раньше окончания предыдущей ролевой группы стори не занимается; ручное смещение старта сохраняет силу. Проверка: тесты проходят.
- [x] 6.5 Тест границы по дате в обоих положениях настройки, включая значение по умолчанию для команды, где настройку ни разу не переключали. Проверка: тесты проходят.
- [x] 6.6 Тест детерминизма: два последовательных пересчёта без изменения входа дают идентичные даты и исполнителей; при нескольких подходящих промежутках выбирается самый ранний. Прогнать многократно (`go test -count=20 ./internal/gantt/`) — защита от зависимости от порядка обхода. Проверка: тест стабилен.
- [x] 6.7 Тест роли без исполнителей: промежуток заполняется, задачи роли между собой не пересекаются. Проверка: тест проходит.
- [x] 6.8 Прогнать `go test ./...` и покрытие по `internal/gantt` и `internal/transport/httpServer/handlers` — убедиться, что покрытие не ниже прежнего. Проверка: тесты зелёные, покрытие не просело.

## 7. Ручная проверка

- [ ] 7.1 Воспроизвести исходный случай на реальных данных: стори с длинным анализом и стори с коротким, одна и та же роль. Проверка: задача менее приоритетной стори встала в простой, более приоритетная не сдвинулась, цепочка за засыпанной задачей уехала влево следом.
- [ ] 7.2 Проверить переключатель в обоих положениях на команде, у которой квартал начался в прошлом. Проверка: при включённой настройке промежутки в прошлом не занимаются, при выключенной занимаются; после переключения диаграмма перерисовывается.
- [ ] 7.3 Проверить, что настройка действует при пересчёте, вызванном не генерацией: изменить прогресс задачи, переупорядочить эпики, сменить исполнителя. Проверка: запрет продолжает действовать во всех трёх случаях.
