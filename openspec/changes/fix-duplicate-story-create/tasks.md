## 1. Frontend: защита от двойной отправки

- [x] 1.1 В `web/gantt/js/scoring-panel.js` (обработчик `btn-create-story`, ~строки 810-835) отключать кнопку (`disabled = true`) сразу в начале обработчика клика, до вызова `apiPost`, и включать обратно (`disabled = false`) в `finally` (после успеха и после ошибки) — так, чтобы повторный клик во время выполнения запроса не мог инициировать второй `apiPost`. Проверить: `node --input-type=module --check web/gantt/js/scoring-panel.js` проходит.
- [ ] 1.2 Ручная проверка в браузере: быстро дважды кликнуть «Добавить историю» с заполненным названием — создаётся ровно одна история; после завершения запроса кнопка снова кликабельна и позволяет создать следующую историю.

## 2. QA

- [ ] 2.1 Проверить сценарии из `specs/epic-scoring-panel/spec.md` (защита от повторной отправки): двойной клик создаёт одну историю, кнопка разблокируется после ответа сервера (в т.ч. после ошибки — например, при пустом названии, если возможно спровоцировать ошибку сервера).
