## 1. Разведка

Пользователь решил не блокировать реализацию на проверке Telegram: сборка SVG и PNG нужна при любом способе отдачи файла, и если браузерное сохранение в Telegram WebApp не заработает, переделывать придётся только последний шаг — способ отдачи, а не всю работу. Проверка в мобильном Telegram вынесена в задачу 4.6.

- [x] 1.1 Проверить **фактически** в десктопном Chrome, что выбранный способ отдачи файла работает: отдать тестовый файл тем же механизмом, который будет использован в 3.4, и убедиться, что он сохраняется. Приложение сегодня отдаёт файлы навигацией на серверный эндпоинт, и в коде отмечено, что это сделано «без fetch+blob» (`reports-panel.js:104-117`) — причина не раскрыта, но выбор не выглядит случайным (design.md, Risks). Проверка: результат зафиксирован; если механизм не работает даже на десктопе — остановиться и вынести вопрос пользователю, не обходить молча. **Результат:** механизм `Blob + URL.createObjectURL + <a download>` фактически проверен через Playwright в реальном десктопном Google Chrome 150 (`channel: 'chrome'`, не встроенный headless-шелл) — SVG- и PNG-блобы сохраняются на диск (`download.saveAs`), файлы валидны (`file` подтверждает формат). Работает без участия сервера — подтверждает design.md Решение 1. **Пересмотрено:** проверка на десктопе оказалась недостаточной — в мобильном Telegram WebApp механизм не сохраняет файл; способ доставки заменён отправкой ботом в чат (design.md Решение 1, задачи 4.x–6.x).

## 2. UX — постановка для фронтенда

- [x] 2.1 Провести UX-ревью и сформировать постановку (субагент `ux`, код не пишет). Что должна закрыть: где разместить управление сохранением и как развести два формата (одна кнопка с выбором или две — решение обосновать); как назвать форматы так, чтобы пользователь понимал разницу без слов «растровый» и «векторный»; обратная связь о ходе подготовки и её завершении, с учётом того, что большая диаграмма готовится заметное время; текст отказа при превышении предела растрового формата — он должен объяснять причину и предлагать выход, а не просто сообщать об ошибке; состояние, когда команда не выбрана. Отдельно применить пункт 9 чек-листа: рядом в тулбаре живут элементы, размер которых зависит от данных, и заявка `stabilize-control-widths` это чинила — не создать новую точку, где раскладка поедет. Проверка: постановка зафиксирована текстом с перечнем состояний и критериями приёмки, согласована с дизайн-системой `web/gantt/css/`. Постановка — `ux-brief.md` в каталоге заявки.

## 3. Frontend — сборка картинки

- [x] 3.1 Собрать самодостаточный SVG из живой диаграммы: копия отрисованного `<svg>` из `#gantt-chart` со вставленным блоком `<style>`, содержащим правила оформления диаграммы и развёрнутые значения токенов (design.md Решение 2). Токены обязательно развернуть: правила используют `var(--color-role-*)`, а объявления живут на `:root` внешнего документа — внутри отдельного файла они окажутся неопределёнными и цвета пропадут. Проверка: сохранённый SVG, открытый в браузере как отдельный файл, показывает цвета ролей, подписи задач с исполнителями и стрелки зависимостей. **Реализовано** в `web/gantt/js/gantt-image-export.js` (`buildExportSvg`/`buildStyleText`/`resolveExportTokens`). Находка, не описанная в design.md: шкала времени (`.upper-text`/`.lower-text`) — это HTML `.grid-header`, СОСЕДНИЙ с `<svg class="gantt">`, а не его часть (сверено по UMD-бандлу и через Playwright); реализован отдельный конвертер `buildHeaderTextGroup`. Проверено визуально через Playwright на реальных CSS-файлах проекта — цвета ролей, подписи с исполнителями, шкала времени, стрелки зависимостей корректны и в SVG, и в растеризованном PNG.
- [x] 3.2 Добавить в экспортируемый SVG подложку цветом фона приложения на всю площадь (design.md Решение 6). Проверка: картинка, вставленная на светлый фон документа, остаётся читаемой. **Реализовано** — `buildRect(0,0,totalWidth,totalHeight,'var(--bg-primary)')` в `buildExportSvg`. Проверено: доминирующий цвет растеризованного PNG — `rgb(11,11,22)` (`--bg-primary`), не прозрачный/чёрный по умолчанию; офлайн-проверка (см. 3.1) подтвердила читаемость без сети.
- [x] 3.3 Воспроизвести легенду в SVG нативными фигурами — прямоугольники и текст, по составу той, что показана над диаграммой (design.md Решение 3). `<foreignObject>` не использовать: браузеры обращаются с ним при растеризации неодинаково. Проверка: в сохранённом файле присутствуют все обозначения из `.gantt-legend`, легенда не перекрывает диаграмму. **Реализовано** — `buildLegendGroup`/`buildLegendItem`/`LEGEND_ITEMS` (состав продублирован из `index.html:186-198` с явным комментарием о риске расхождения). Проверено: все 10 обозначений присутствуют и читаемы в PNG; перенос строк при узкой диаграмме (900px, `infinite_padding:false`) работает без наложения на диаграмму, `legend.height` корректно резервирует место под все строки.
- [x] 3.4 Реализовать сохранение векторного формата: отдать собранный SVG пользователю способом, подтверждённым в 1.1. Проверка: файл сохраняется, открывается вне приложения, имя позволяет отличить команду и момент. **Реализовано** — `exportGanttSvg`/`downloadBlob`. Проверено через Playwright: файл сохраняется, открывается как отдельный документ, имя вида `gantt-<команда>-<YYYY-MM-DD_HH-mm>.svg`. **Пересмотрено:** способ отдачи `downloadBlob` заменяется отправкой в чат задачей 6.1; сборка SVG и имя файла остаются.
- [x] 3.5 Реализовать сохранение растрового формата отрисовкой собранного SVG на холст (design.md Решение 4) — общий источник с векторным, а не вторая независимая реализация. Проверка: растровый и векторный файлы одной диаграммы совпадают по содержимому; растровый открывается в просмотрщике изображений. **Реализовано** — `exportGanttPng`/`rasterizeSvgToCanvas` строится из того же `buildExportSvg()`. Проверено: PNG валиден (`file` подтверждает формат), цвета/текст/легенда/состояния совпадают с SVG-версией той же диаграммы. По ходу проверки найдены и исправлены три дефекта, не описанных в design.md/spec.md: отсутствие `.grid-background{fill:none}` и `.handle{opacity:0}` в перенесённых правилах (без них — сплошная чёрная заливка поверх сетки) и SMIL `<animate>` из Frappe Gantt (клонируются вместе с DOM и проигрываются заново при каждом открытии/растеризации — для однокадрового PNG это грозило недетерминированно обрезанными барами; узлы анимации удаляются после клонирования).
- [x] 3.6 Проверять пределы холста **до** растеризации и при превышении не сохранять файл, а показывать объяснение причины с предложением сменить масштаб или сохранить вектор (design.md Решение 5). Фактические пределы взять замером в целевых окружениях, а не из справочника: на мобильных они заметно ниже. Проверка: диаграмма, превышающая предел, даёт понятный отказ, а не обрезанный или пустой файл; тот же случай в векторном формате сохраняется успешно. **Пределы измерены фактически** в десктопном Chrome 150 через Playwright (см. отчёт задачи) — 65535px на сторону, 268 435 456px² (16384×16384) по площади; константы `MAX_CANVAS_DIMENSION_PX`/`MAX_CANVAS_AREA_PX`. Проверено: диаграмма ~164 790px шириной даёт `showErrorModal` с текстом причины и обоими выходами, PNG не создаётся (`download` не срабатывает), SVG для той же диаграммы сохраняется успешно.
- [x] 3.7 Реализовать обратную связь и защиту от повторного запуска: индикация подготовки, сообщение о завершении, блокировка управления на время работы со снятием в `finally`, человеко-понятный текст ошибки. Проверка: повторное нажатие во время подготовки не запускает вторую; блокировка снимается и на ветке ошибки. **Реализовано** — `runExport` (паттерн `runRegenerateQuarter` из `app.js`), `withSubmitLock([btnPng, btnSvg], ...)`. Проверено: успешный экспорт даёт тост «✅ Картинка сохранена»; отказ по пределу холста не даёт тост-подтверждение (модалка уже объяснила причину); кнопки разблокируются и подпись восстанавливается в обоих случаях (`finally`). **Пересмотрено:** текст успеха «Картинка сохранена» теперь ложен — заменяется по постановке 5.1 задачей 6.2.
- [x] 3.8 Сделать сохранение доступным любому, кому доступна диаграмма, включая роль `member` — сохранение ничего не меняет в данных. Управление недоступно, пока команда не выбрана и диаграмма не построена. Проверка: под `member` сохранение работает; без выбранной команды недоступно. **Реализовано** — кнопки не входят в список, скрываемый для `member` (app.js, с явным поясняющим комментарием); доступность переключается в `gantt-renderer.js::renderGantt` по `tasks.length === 0` — единая точка истины с `#gantt-empty`/`#task-count`.
- [x] 3.9 Реализовать остальные находки UX-постановки из 2.1, относящиеся к подаче. Проверка: соответствует критериям приёмки постановки. **Реализовано**: разметка `.gantt-export-actions` в `.toolbar-right` после `#task-count` (index.html), иконки/подписи/`title` по тексту постановки без слов «растровый»/«векторный», `.gantt-export-actions{flex-basis:100%}` в `@media(max-width:480px)` (responsive.css), стили `.gantt-export-actions` в gantt.css.

## 4. Backend — отправка картинки в чат

Контракт — design.md Решение 7. Реализация — субагент `backend`.

- [x] 4.1 Добавить в `*telegram.Bot` метод отправки документа в личный чат по `chat_id` (имя файла, байты, подпись) и узкий интерфейс отправки документа в `handlers/interfaces.go` по образцу `TelegramNotifier`. Ответ Telegram 403 (бот заблокирован / чат не начат) отдавать распознаваемой ошибкой (sentinel/тип), а не строкой. Проверка: `*telegram.Bot` удовлетворяет интерфейсу структурно, без импорта пакета `telegram` в `handlers`; `go build ./...` проходит. **Реализовано** — `(*Bot).SendDocumentToChat` (`internal/telegram/telegramBot.go`) и интерфейс `handlers.DocumentSender` (`internal/transport/httpServer/handlers/interfaces.go`). Для различения 403 handler не импортирует `internal/telegram`: `SendDocumentToChat` пробрасывает ошибку Bot API через `%w` как есть, а библиотека `github.com/go-telegram/bot` сама оборачивает 403 в свой экспортируемый сентинел `bot.ErrorForbidden` (см. `raw_request.go` библиотеки) — `handlers/gantt_export.go` проверяет её через `errors.Is(err, tgbot.ErrorForbidden)`, импортируя напрямую сторонний пакет `github.com/go-telegram/bot` (уже зависимость проекта, используется внутри `internal/telegram`), а не `internal/telegram`. Выбрано это, а не сентинел в `handlers`, чтобы не заставлять `internal/telegram` (сам по себе транспортный слой, как и `handlers`) импортировать пакет HTTP-хендлеров ради одной переменной-ошибки. `go build ./...` проходит.
- [x] 4.2 Внедрить отправитель в `GanttHandler` в `app/main.go` тем же способом, что `notifier`: nil, если бот не поднялся (`app/main.go:94-101`). Проверка: приложение стартует и с ботом, и без него. **Реализовано** — `docSender handlers.DocumentSender` в `app/main.go`, тот же паттерн и тот же typed-nil комментарий, что у `notifier`; внедряется в `GanttHandler` методом `WithDocumentSender` (`internal/transport/httpServer/handlers/gantt.go`), а не обязательным параметром `NewGanttHandler` — по образцу уже существующего `WithReportServices`, чтобы не менять сигнатуру конструктора и не задевать ~150 существующих вызовов в тестах пакета `handlers`.
- [x] 4.3 Реализовать handler `POST /api/gantt/export/image` строго по таблице Решения 7: nil-guard → `NOTIFIER_UNAVAILABLE`; `http.MaxBytesReader` 50 МБ → `FILE_TOO_LARGE`; `format` → `INVALID_FORMAT`; сигнатура PNG / начало SVG → `INVALID_FILE`; санитизация `filename` с запасным именем и расширением по `format`; получатель — `chat_id` пользователя по `session.TelegramID`, `0` или 403 от Telegram → `BOT_CHAT_NOT_STARTED`; прочий сбой → `TELEGRAM_SEND_FAILED` с логированием причины; успех → `200 {"status":"sent"}`. Адресат НЕ берётся из запроса. Проверка: все ошибки — в формате `{ "error": { "code", "message" } }`. **Реализовано** — `GanttHandler.ExportGanttImage` (`internal/transport/httpServer/handlers/gantt_export.go`), ровно в этом порядке проверок; `contentMatchesFormat` (сигнатура PNG / начало SVG после BOM и пробелов) и `sanitizeExportImageFilename` (оставляет буквы любого алфавита/цифры/`-_. `, запасное имя `gantt-chart`, расширение всегда = `format`) — там же.
- [x] 4.4 Зарегистрировать роут в группе под `TelegramAuth` (`routers.go`), без ограничения по роли — выгрузка доступна `member`. Проверка: без авторизации — 401; под `member` — не 403. **Реализовано** — `POST /export/image` в `internal/transport/httpServer/routers/routers.go`, внутри группы `TelegramAuth`, без вложенной группы `RoleAuth` (в отличие от admin-эндпоинтов рядом).
- [x] 4.5 Покрыть handler Go-тестами с подменой отправителя и репозитория (по образцу `notify_test.go`): каждая строка таблицы Решения 7 плюс успех; отдельно — что адресат берётся из сессии, а не из тела запроса. Проверка: `go test ./internal/transport/httpServer/...` зелёный. **Реализовано** — `internal/transport/httpServer/handlers/gantt_export_test.go`, 13 сценариев: все шесть кодов ошибок таблицы, успех, адресат из сессии (два пользователя с одинаковым телом запроса получают файл на разные `chat_id`), 401 без сессии, санитизация/запасное имя файла. `go test ./internal/transport/httpServer/... ./internal/telegram/...` зелёный.
- [x] 4.6 Описать контракт эндпоинта в `.agents/tasks/task_backend.md`. Проверка: коды и поля совпадают с Решением 7. **Реализовано** — раздел «Шаг 15. Выгрузка картинки диаграммы Ганта в чат» в `.agents/tasks/task_backend.md`.

## 5. UX — постановка на выгрузку в чат

- [x] 5.1 Доработать `ux-brief.md` (субагент `ux`, код не пишет) под доставку ботом. Что должна закрыть: подписи кнопок и `aria-label`/`title` — действие теперь «отправить в чат», а не «сохранить», при сохранении различимости форматов без слов «растровый»/«векторный» и без поломки раскладки тулбара на ≤480px (пункт 9 чек-листа); индикация на двух этапах — подготовка и отправка; текст успеха, называющий, где искать файл (спека: «Успешная выгрузка указывает, где файл»); тексты для `BOT_CHAT_NOT_STARTED` (какое действие назвать и можно ли дать ссылку на чат с ботом — проверить, известно ли фронтенду имя бота), `FILE_TOO_LARGE` (предложить более крупный масштаб; различить с отказом по пределу холста), `TELEGRAM_SEND_FAILED` и `NOTIFIER_UNAVAILABLE`; канал показа каждой ошибки (тост или `showErrorModal`). Проверка: постановка перечисляет состояния и критерии приёмки; если находка требует изменения продуктовых требований, а не подачи — вынести вопрос пользователю. **Выполнено** — `ux-brief.md`, редакция 2: видимый текст кнопок не меняется, действие — в `title`/`aria-label`; две фазы «⏳ Подготовка…»/«⏳ Отправка…»; успех «Файл отправлен в чат с ботом» только после `200`; карта `error.code` → текст/канал (модалка — `BOT_CHAT_NOT_STARTED`, предел холста, 50 МБ; тост — остальное). Имя бота фронтенду неизвестно — в `BOT_CHAT_NOT_STARTED` действие названо словами, ссылка на чат вынесена на решение пользователя.

## 6. Frontend — выгрузка через бота

Реализация — субагент `frontend` по постановке 5.1.

- [x] 6.1 Заменить `downloadBlob` в `gantt-image-export.js` на отправку собранного файла на `POST /api/gantt/export/image` multipart-запросом (`FormData`: `file`, `format`, `filename` из `buildExportFileName`) с теми же заголовками авторизации, что у `api.js`; существующий `apiPost` шлёт только JSON — добавить рядом хелпер для multipart, а не обходить авторизацию. До отправки проверять размер файла против 50 МБ и при превышении не отправлять. Удалить `downloadBlob` и ставшие мёртвыми комментарии о нём. Проверка: оба формата уходят запросом; сборка SVG/PNG (задачи 3.1–3.6) не изменена. **Реализовано** — `apiPostMultipart` (`api.js`): переиспользует `getHeaders()`/`handleHttpError` как `apiGet`/`apiPost`/`apiPut`/`apiDelete`, явно удаляет `Content-Type` из скопированных заголовков перед `fetch(FormData)`, чтобы boundary подставил браузер. `sendExportImage` (`gantt-image-export.js`) собирает `FormData` (`file`/`format`/`filename`) и вызывает его. `buildGanttSvgFile`/`buildGanttPngFile` (бывшие `exportGanttSvg`/`exportGanttPng`) теперь только строят `{ blob, filename }`, не отдают файл; `downloadBlob` и комментарий о его происхождении (задача 1.1) удалены. Клиентский предел `MAX_UPLOAD_FILE_BYTES = 50 * 1024 * 1024` проверяется в `runExport` до вызова `sendExportImage`. Сборка SVG/PNG (`buildExportSvg`, легенда, токены, `rasterizeSvgToCanvas`, пределы холста) не менялась. Проверено через Playwright (Chromium 1091, реальный браузер, локальный статик-сервер + `page.route`/перехват `fetch` через `page.addInitScript`): у исходящего запроса `Content-Type: multipart/form-data; boundary=...` выставлен браузером, `Authorization: Bearer <token>` присутствует и совпадает с `localStorage.tg_sys_token`, `FormData` содержит `file` (Blob нужного MIME и размера), `format` (`png`/`svg`) и `filename`, совпадающий с `buildExportFileName`.
- [x] 6.2 Реализовать подачу по постановке 5.1: подписи кнопок, индикация подготовки и отправки в рамках существующего `runExport`/`withSubmitLock`, текст успеха, обработка каждого `error.code` из Решения 7 выбранным каналом. Успех показывается только после `200`. Проверка: ни одна ветка ошибки не показывает сообщение об успехе; блокировка снимается в `finally` на всех ветках. **Реализовано** — `title`/`aria-label` кнопок `btn-export-gantt-png`/`btn-export-gantt-svg` обновлены в `index.html` дословно по ux-brief.md §1 (видимый текст и `id` не менялись). `runExport` (`gantt-image-export.js`) расширен до двух фаз: `⏳ Подготовка…` до вызова `buildFn`, `⏳ Отправка…` непосредственно перед `sendExportImage` (сетью); при отказе фазы 1 (ошибка сборки или `'limit-exceeded'` от `buildGanttPngFile`) фаза «Отправка» не наступает. Успех `showToast('Файл отправлен в чат с ботом', 'success')` — единственное место показа, стоит сразу после `await sendExportImage(...)`, т.е. строго после `200 {"status":"sent"}`; любой более ранний `return` (ошибка сборки, предел холста, предел 50 МБ) или исключение из `sendExportImage` (перехватывается `catch` → `handleExportError`) этот код не достигает. `handleExportError` — карта `err.code` → {текст, канал} по ux-brief §4: `BOT_CHAT_NOT_STARTED`/`FILE_TOO_LARGE` → `showErrorModal` (для `FILE_TOO_LARGE` тот же текст `FILE_TOO_LARGE_MESSAGE`, что и у клиентской проверки 50 МБ), `TELEGRAM_SEND_FAILED`/`NOTIFIER_UNAVAILABLE` → `showToast(..., 'error')` каждый со своим текстом, `INVALID_FORMAT`/`INVALID_FILE`/`internal_error`/`INVALID_REQUEST_BODY`/сетевой сбой без кода/любой не перечисленный код → общий `showToast` без деталей (§4.6). Ошибка сборки (фаза 1, без сети) обрабатывается отдельным `catch` внутри `runExport` до объявления `handleExportError` и сохраняет прежний текст «Не удалось подготовить картинку…» (§4.7, без изменений). `activeBtn.innerHTML = originalLabel` — в едином `finally`, покрывающем все ветки (успех, отказ фазы 1, `limit-exceeded`, отказ 50 МБ, отказ фазы 2). Проверено через Playwright (Chromium 1091, `page.route` с разными `status`/`error.code`): сценарии 200 (PNG и SVG) → только тост успеха, кнопка разблокирована, подпись восстановлена; `BOT_CHAT_NOT_STARTED` (409) → модалка с текстом об открытии чата, тоста успеха нет; `FILE_TOO_LARGE` (413) → модалка с текстом про 50 МБ; `TELEGRAM_SEND_FAILED` (502) и `NOTIFIER_UNAVAILABLE` (500) → тост с соответствующим текстом; `internal_error` (пример «непредвиденного» кода) → общий тост §4.6; отдельный сценарий с завышенным `width` живого SVG (клиентский предел холста, без сети) → модалка с обновлённым текстом §4.2 («холста браузера» / «отправьте диаграмму в формате SVG»), запрос к `/api/gantt/export/image` не отправляется (проверено флагом в `page.route`). Во всех сценариях кнопка по завершении не заблокирована, подпись — исходная иконка/название формата, посторонних `pageerror` в консоли нет.

## 7. Проверка

Клиентская часть автотестами не покрывается (JS-раннера нет, design.md Risks) — проверки 7.2–7.7 ручные, по сценариям `specs/gantt-chart-image-export/spec.md`.

- [x] 7.1 Прогнать `go test ./...` и `go vet ./...` (субагент `qa`), сверить покрытие handler'а 4.3 по строкам таблицы Решения 7. Проверка: всё зелёное, непокрытых кодов ошибок нет. **Выполнено** — `go vet ./...` и `go test ./...` зелёные (субагент `qa`). Сверка с таблицей Решения 7 выявила непокрытые `INVALID_REQUEST_BODY` и `INVALID_FILE` «файл не передан» — добавлены подтесты `not_multipart_returns_400_invalid_request_body`, `missing_file_returns_400_invalid_file`; все строки таблицы и успех покрыты, `ExportGanttImage` — 89.3% операторов. Непокрытой осталась только ветка ошибки `io.ReadAll` вне `MaxBytesError` (в unit-тесте не воспроизводится: к этому моменту форма уже разобрана `ParseMultipartForm`) и недостижимый `default` в `contentMatchesFormat`.
- [ ] 7.2 Пройти сценарии требования «В картинку попадает вся диаграмма»: диаграмма шире экрана, задач больше, чем помещается по высоте, разная позиция прокрутки перед выгрузкой. Проверка: содержимое картинки не зависит от прокрутки, всё внутри.
- [ ] 7.3 Пройти сценарии требования «Картинка соответствует выбранному масштабу»: выгрузить в «Дне» и в «Месяце». Проверка: различается цена деления шкалы, а не только размер.
- [ ] 7.4 Пройти сценарии требований «Картинка самодостаточна вне приложения» и «Легенда входит в картинку» на диаграмме, где есть завершённые задачи, задачи без исполнителя и с недействующим закреплением — открывая файлы, пришедшие в чат. Проверка: все состояния выглядят как на экране; состав легенды совпадает с интерфейсом; фон залит; подписи читаются без доступа к сети.
- [ ] 7.5 Пройти сценарии требования «Слишком большая диаграмма не даёт молча испорченный файл»: подобрать команду и масштаб, выходящие за предел холста. Проверка: отказ понятен и предлагает выход, в чат ничего не приходит; вектор для той же диаграммы приходит в чат.
- [ ] 7.6 Пройти сценарии требований «Недоставленная картинка не выдаётся за доставленную», «Выгрузка доступна всем, кто видит диаграмму» и «Ход и итог выгрузки понятны пользователю»: пользователь без начатого чата с ботом и заблокировавший бота; бот не поднят; файл больше предела отправки; под `member`; без выбранной команды; повторное нажатие во время подготовки и отправки. Проверка: ни в одном отказе нет сообщения об успехе; сообщение об успехе называет чат с ботом.
- [ ] 7.7 Проверить оба формата в мобильном Telegram WebApp — основном клиенте приложения: файлы приходят в личный чат, открываются и остаются в истории. Там же замерить фактические пределы холста (design.md Risks) и при расхождении с `MAX_CANVAS_DIMENSION_PX`/`MAX_CANVAS_AREA_PX` понизить константы до меньшего из замеренных значений. Проверка: оба файла получены; пределы зафиксированы в этой задаче.
