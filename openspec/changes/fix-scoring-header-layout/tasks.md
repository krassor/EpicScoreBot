## 1. UX — постановка (субагент `ux`)

- [x] 1.1 Зафиксировать контрольные ширины для проверки переноса и снять с них ожидаемую раскладку шапки (широкий десктоп, ширина, на которой группа контролов перестаёт помещаться, 768px, 480px) — приёмка: для каждой ширины описано, где стоит группа контролов и как выровнена
- [x] 1.2 Определить целевой размер мелких контролов: вертикальные отступы, `min-height` и размер шрифта, согласованные с существующим `.btn-sm` (`padding: 6px 12px`, `font-size: var(--font-size-xs)`, `min-height: 30px`), плюс парные значения для `input` и `.badge`, чтобы все три вставали на одну линию — приёмка: значения заданы числами или токенами, а не словами «как у кнопки»
- [x] 1.3 Проверить по коду структуру шапки истории на совпадение с шапкой эпика по вложенности (заголовок первым потомком, группа контролов последним) — приёмка: подтверждено, что селектор `.scoring-details-header > div:last-child` из `responsive.css` попадёт в группу контролов, либо названо, что именно надо изменить в разметке

## 2. Frontend — дизайн-система (`css/components.css`)

Выполняется первой: разметка в секции 3 опирается на эти классы (см. `design.md`, Migration Plan).

- [x] 2.1 Завести класс размера мелких контролов со значениями из 1.2 и парный вариант для `input` — приёмка: кнопка, поле и бейдж с этими классами имеют одинаковую вычисленную высоту
- [x] 2.2 Завести класс фиксированной ширины числового поля оценки (70-80px, см. `design.md`, Decision 4) — приёмка: ширина задаётся классом, инлайновый `width` в разметке не нужен
- [x] 2.3 Привести `.badge` в строках действий к той же высоте, что кнопка и поле — приёмка: бейдж «Финальная оценка» не выше и не ниже соседних контролов

## 3. Frontend — шапки деталей (`js/scoring-panel.js`, `css/responsive.css`)

- [x] 3.1 Перевести шапку истории (`renderStoryDetailsHtml`, ~1149-1162) на класс `.scoring-details-header` вместо инлайновой раскладки; масштаб уровня (`h3`, меньшие размеры) выразить вложенным классом, не переопределяя раскладку — приёмка: инлайновых `display: flex` / `justify-content` / `align-items` в шапке истории не осталось
- [x] 3.2 Убедиться, что адаптивное правило `css/responsive.css` (~232-239) действует на обе шапки, и при необходимости расширить селектор — приёмка: на ≤768px шапка истории складывается в колонку с выравниванием группы контролов по левому краю, как у эпика
- [x] 3.3 Снять инлайновые `padding`/`font-size` с кнопок шапки эпика (~463-465) и заменить на классы из секции 2; добавить `flex-wrap: wrap` контейнеру строки заголовка (~461), потому что рост кнопок до `.btn-sm` (+~24px на три кнопки) повышает риск переполнения этой строки при длинном названии — приёмка: `.btn-sm` больше не перебивается инлайном, кнопки одной высоты, при длинном названии эпика строка переносится, а не выходит за карточку
- [x] 3.4 Снять инлайновые размеры со строки действий шапки истории — поле оценки, «Пересчитать по формуле», «Переопределить», бейдж (~1134, 1141-1143); добавить `flex-wrap: wrap` внутреннему контейнеру строки (~1140), иначе на 480px она упирается в ширину карточки — приёмка: контролы выровнены по общей линии, инлайновых размеров в блоке нет, на 480px строка не выходит за границы карточки

## 4. Frontend — однотипные контролы панели

- [x] 4.1 Снять инлайновые размеры в блоке экспертной оценки роли (~692-693) и заменить на классы из секции 2 — приёмка: поле и кнопка одной высоты
- [x] 4.2 Снять инлайновые размеры в блоке переопределения оценки роли (~734-735) — приёмка: поле и кнопка одной высоты
- [x] 4.3 Снять инлайновые размеры у кнопок оценки риска (~1044, 1078) — приёмка: кнопки того же размера, что и в остальных строках действий
- [x] 4.4 Привести к тем же классам оставшиеся строки действий: таблица оценок по ролям — поле и кнопки «Ок»/«Отмена»/«Изменить` (~953-969), строка голосования за сторю (~1178-1179) — приёмка: контролы этих блоков той же высоты, что в шапках
- [x] 4.5 Проверить, что в `js/scoring-panel.js` не осталось инлайновых `padding`/`font-size` у контролов строк действий — приёмка: `grep -nE 'class="[^"]*(btn |btn-|input)[^"]*"[^>]*style="[^"]*(padding|font-size) *:' js/scoring-panel.js` находит только иконочные кнопки без класса `.btn` (удаление истории, редактирование риска) и одиночную полноширинную «Добавить историю» — они не стоят в строке с другими контролами и спеком не покрыты

## 5. QA — верификация (субагент `qa`)

- [x] 5.1 Проверить шапку истории и шапку эпика на ширинах из 1.1 — приёмка: по каждой ширине зафиксировано отсутствие наложения элементов и положение группы контролов
- [ ] 5.2 Проверить историю с длинным названием без пробелов и с длинным описанием — приёмка: текст переносится внутри колонки, группа контролов не выходит за границы контейнера
- [x] 5.3 Сравнить вычисленную высоту контролов в четырёх блоках (шапка истории, шапка эпика, экспертная оценка роли, переопределение оценки роли) — приёмка: у однотипных контролов высота совпадает
- [x] 5.4 Проверить блоки, не показанные на скриншотах, на непреднамеренные изменения после снятия инлайна — таблица оценок по ролям, блок рисков — приёмка: изменения ограничены размером контролов, раскладка блоков не поехала

## 6. UX — приёмочное ревью (субагент `ux`)

- [ ] 6.1 Провести ревью по критериям приёмки спека `scoring-panel-layout` — приёмка: по каждому требованию зафиксировано «выполнено» либо конкретное расхождение
