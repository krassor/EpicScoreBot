## 1. Frontend: фильтры списка эпиков

- [x] 1.1 В `web/gantt/js/scoring-panel.js` добавить над списком «Эпики команды» (перед вызовом `renderEpicsList`) три `<select>`: «Год», «Квартал», «Тип эпика» — каждый с первым пунктом «Все» (`value=""`), выбранным по умолчанию. Квартал — фиксированные Q1–Q4 (как в `edit-epic-quarter`), тип — фиксированные `feature`/`architecture`/`techdebt` с подписями «Feature»/«Architecture»/«Techdebt» (как в `edit-epic-type`). Проверить: `node --input-type=module --check web/gantt/js/scoring-panel.js` проходит.
- [x] 1.2 Список опций фильтра «Год» строить динамически как отсортированное множество различных `epic.year` из загруженного списка эпиков команды (плюс «Все» первым пунктом) — пересобирать при каждой загрузке списка эпиков (`loadScoringEpics`). Проверить: при разных наборах эпиков (один год / несколько лет) список опций фильтра соответствует фактическим годам.
- [x] 1.3 Реализовать функцию клиентской фильтрации: применяется к уже загруженному массиву эпиков перед вызовом `renderEpicsList`, комбинирует все три фильтра по И (эпик проходит, если для каждого фильтра выбрано «Все» либо значение совпадает с полем эпика `year`/`quarter`/`type`). Изменение любого из трёх `<select>` пересчитывает и перерисовывает список без повторного запроса к серверу. Проверить вручную в браузере: выбор одного фильтра сужает список; комбинация нескольких фильтров работает по И; возврат к «Все» снимает ограничение.
- [x] 1.4 Сохранить состояние фильтров при переключении команды: при смене команды (новый вызов `loadScoringEpics` для другого `teamId`) фильтры сбрасываются к «Все» (список эпиков — новый набор для другой команды, старые значения фильтров могут не иметь смысла для нового набора годов).

## 2. QA

- [ ] 2.1 Проверить сценарии из `specs/epic-list-filters/spec.md`: фильтрация по одному параметру, комбинация нескольких, значение «Все» по умолчанию и после возврата, состав опций фильтра «Год» соответствует фактическим данным.
