## 1. Backend: пустой список историй — не null

- [x] 1.1 В `internal/repositories/epic.go`, `GetStoriesByEpicID` — заменить `var epics []domain.Epic` на `epics := []domain.Epic{}`, чтобы при отсутствии строк в результате запроса функция возвращала пустой срез, а не `nil` (JSON-сериализация — `[]`, а не `null`)
- [x] 1.2 Проверить `go build ./...`, `go vet ./...`; если есть тесты на `GetStoriesByEpicID`/`GetStories`-хендлер — прогнать и убедиться что не сломались; при отсутствии — не обязательно добавлять новый тест специально под эту задачу (тривиальная правка), но не помешает

## 2. Frontend: защита от null-ответа и уточнённая заглушка

- [x] 2.1 В `web/gantt/js/scoring-panel.js`, `loadEpicData()` — добавить защиту от `null`/не-массива для `currentStories` (например, `currentStories = (await apiGet(...).catch(() => ([]))) || []`), а также для остальных используемых в этой функции ответов (`selectedStoryScores`/`selectedEpicScores` — гарантировать объект с полями `scores`/`expected`/`received`, если ответ `null`; `selectedStoryRoleScores`/`selectedEpicRoleScores` — гарантировать массив; `risksData`/`.risks` — гарантировать, что обращение к `.risks` не падает, если сам `risksData` оказался `null`, а не только когда `.risks` внутри него `null`)
- [x] 2.2 В `renderDetails()` — для случая `currentStories.length === 0` изменить текст правой колонки (сейчас «👈 Выберите историю (сторю) слева для голосования и оценки рисков», показывается при любом `!selectedStory`) на отдельное сообщение специально для пустого списка — например, поясняющее, что историй пока нет, и (если админ и статус NEW) что их можно создать через форму слева; для случая, когда истории ЕСТЬ, но просто ничего не выбрано, — оставить текущий текст как есть
- [x] 2.3 Убедиться, что кнопка «✏️ Редактировать» (`btn-edit-epic`, рендерится в шапке `renderDetails()`) продолжает отображаться и работать для эпика без историй — она уже не зависит от `currentStories`/`selectedStory`, специальных правок скорее всего не требуется, но нужно подтвердить чтением кода после правок 2.1-2.2, что ничего не поломало эту часть рендера
- [x] 2.4 Проверить синтаксис (`node --input-type=module --check < web/gantt/js/scoring-panel.js`)

## 3. QA

- [ ] 3.1 Проверить вручную (браузер, после деплоя): открыть в «Скоринге» эпик без историй — лоадер сменяется содержимым, ошибки в toast нет
- [ ] 3.2 Проверить: правая колонка показывает новую заглушку про отсутствие историй, а не «Выберите историю слева»
- [ ] 3.3 Проверить: кнопка «Редактировать» видна и открывает форму редактирования эпика для этого же эпика без историй
- [ ] 3.4 Регрессия: эпик С историями по-прежнему открывается корректно, выбор истории из списка работает как раньше
