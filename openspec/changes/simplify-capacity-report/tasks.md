## 1. Backend: округление и общее ядро данных для PDF

- [x] 1.1 Реализовать в пакете `internal/report` общий helper округления (поячеечный `ceil` риск-скорректированной трудоёмкости эпик×роль, «Итого» по эпику и «Запланировано» по роли — суммы уже округлённых ячеек, см. псевдокод в `design.md` раздел «Decisions» п.3) и покрыть юнит-тестами (дробные значения, суммы сходятся с отображаемыми слагаемыми)
- [x] 1.2 Проверить по всему репозиторию usages `report.ReportData`/`EpicReportData` (`TotalScore`, `RoleScoresMap`, `RoleScores`) вне `internal/report`/`config/template.html`/`admin_callbacks.go`/`reports.go`, чтобы не сломать скрытых потребителей (см. риск в `design.md`)
- [x] 1.3 Переписать `epicService.GetReportData` (`internal/services/epic.go`): внутри вызывать `BuildCapacityReport` для ядра (команда/период/ёмкость/квоты/эпики с `role_scores`/`raw_role_scores`), встроить `CapacityReportResponse` в `report.ReportData` (embedding), поверх — дозапросить риски по эпику и `Generated`; сигнатура метода не меняется
- [x] 1.4 Убедиться, что оба вызывающих (`internal/telegram/admin_callbacks.go:657`, `internal/transport/httpServer/handlers/reports.go:150`) продолжают компилироваться и работать без изменений на своей стороне
- [x] 1.5 Прогнать `go build ./...` и существующие тесты (`internal/services/epic_test.go`, `internal/services/capacity_report_test.go`) — обновить моки/ожидания под новую реализацию `GetReportData`, где это необходимо

## 2. Backend: XLSX-генератор

- [x] 2.1 В `internal/report/xlsx_generator.go` (`writeCapacitySheet`) переключить колонки по ролям с `e.RawRoleScores` на округлённые (helper из 1.1) `e.RoleScores`; объединить «Итого (чд)»/«С рисками (чд)» в одну колонку «Итого (чд)»
- [x] 2.2 Итоговые строки «Запланировано»/«Доступно»/«Разница» — «Запланировано» считать суммой округлённых значений по роли (не пересчитывать отдельно), «Доступно»/«Разница» оставить как есть (не риск-скорректированная трудоёмкость)
- [x] 2.3 Лист «Эпики» (`writeEpicsSheet`) не менять по составу колонок (сырые `raw_role_scores` остаются для аудита) — проверить, что рефакторинг 1.3 не изменил его данные

## 3. Backend: PDF-шаблон

- [x] 3.1 Обновить `config/template.html`: главная матрица — одна колонка на роль (округлённая риск-скорректированная), одна «Итого (чд)» на эпик, без отдельной «С рисками (чд)»
- [x] 3.2 Переименовать подпись «Сумма оценок (без рисков)» в карточке эпика на корректную (значение риск-скорректированное) — без изменения структуры карточки/таблицы рисков/SVG-диаграмм
- [ ] 3.3 Вручную сгенерировать PDF (через существующий тестовый вызов/локальный Gotenberg) для команды с несколькими эпиками и проверить визуально: числа в матрице совпадают с веб-таблицей и XLSX для того же team_id/year/quarter

## 4. Frontend: веб-таблица «Отчёты»

- [x] 4.1 В `web/gantt/js/reports-panel.js` (`renderCapacityTable`) реализовать helper округления, зеркалирующий алгоритм из 1.1 (см. псевдокод в `design.md`), и переключить колонки по ролям с `epic.raw_role_scores` на округлённые `epic.role_scores`
- [x] 4.2 Убрать колонку «С рисками (чд)», оставить одну «Итого (чд)» = сумма округлённых ячеек строки; итоговую строку «Запланировано» считать суммой округлённых значений по роли (не через `rc.planned` напрямую, если это меняет согласованность с отображаемыми ячейками — свериться с 1.1)
- [x] 4.3 Обновить блок формул в `web/gantt/index.html` (`reports-legend`) под новый набор колонок и правило округления вверх — текст должен соответствовать `specs/capacity-report/spec.md` (`MODIFIED Requirements`)
- [ ] 4.4 Проверить в браузере на реальных/тестовых данных: таблица отображает только риск-скорректированные округлённые значения, суммы по строкам/колонкам визуально сходятся

## 5. QA

- [x] 5.1 Юнит-тесты helper'а округления (Go, задача 1.1): дробные значения на границе (`.0`, `.1`, `.9`), сумма округлённых ячеек равна отображаемому итогу, квоты продолжают считаться по точным `final_score`
- [x] 5.2 Тест на согласованность данных между JSON `/reports/capacity`, XLSX-выгрузкой и PDF для одного team_id/year/quarter — риск-скорректированные и округлённые значения совпадают во всех трёх форматах (см. требование «Согласованность данных…» в спеке)
- [ ] 5.3 Регрессионная проверка Telegram-бота: команда `/report` (`admin_callbacks.go`) генерирует PDF без ошибок после рефакторинга `GetReportData`, содержимое соответствует новой форме матрицы (юнит-уровень `GetReportData` покрыт на 95%; сквозной прогон через реальный Telegram/Gotenberg не выполнялся — нужна ручная проверка)
- [x] 5.4 `go test ./...` проходит, `go tool cover` — покрытие нового кода не хуже принятого в проекте порога
