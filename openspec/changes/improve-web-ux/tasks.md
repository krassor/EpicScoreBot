## 1. UX — постановка и подбор палитры (субагент `ux`)

UX-ревью всех экранов `web/gantt/` уже проведено — его результат лёг в основу `proposal.md` и спеков. Здесь остаётся то, что должно быть решено до правки кода.

- [x] 1.1 Подобрать конкретные значения ролевых цветов (`--color-role-*`) и структурных (`--color-epic`, `--color-story`, `--color-default`), не пересекающиеся с семантическими и статусными токенами; проверить набор через `validate_palette.js --mode dark` из скилла `dataviz` и приложить вывод проверки — приёмка: PASS по CVD separation и normal-vision floor, ни одно значение не равно `--color-success`/`--color-warning`/`--color-info`/`--color-danger`/`--color-status-*`
- [x] 1.2 Составить поимённый список всех модальных окон (`index.html` + создаваемые из JS) с указанием, какие из них содержат деструктивное действие — приёмка: список покрывает все места вызова модалок, каждая помечена «деструктивная / обычная» для решения по порядку кнопок
- [x] 1.3 Сформулировать тексты для новых состояний: модалка удаления истории (с перечислением каскадно удаляемых рисков и оценок), подтверждение снятия администратора команды, ошибка загрузки таблицы пользователей, ошибка загрузки администраторов команды, экран «Доступ ограничен» с действиями, состояние «не удалось подключиться» — приёмка: тексты на русском, каждый отличим от соседнего штатного состояния

## 2. Frontend — общие утилиты (`js/utils.js`)

Выполняется первым: остальные секции опираются на эти функции (см. `design.md`, Migration Plan).

- [x] 2.1 Добавить `escapeHtml(text)` — приёмка: строка `<b>a</b> & b < c` возвращается полностью экранированной, повторный вызов на уже экранированной строке не требуется нигде в кодовой базе
- [x] 2.2 Добавить `withSubmitLock(form, asyncFn)` — блокировка submit-кнопки на время запроса со снятием в `finally`; приёмка: при исключении внутри `asyncFn` кнопка снова доступна
- [x] 2.3 Добавить `handleApiError(err, { title, blocking })` — прогон через `KNOWN_ERROR_MESSAGES` и выбор канала (модалка для причин, требующих действий пользователя; тост для остального); приёмка: известное сообщение показывается по-русски, неизвестное — как есть, без падения
- [x] 2.4 Расширить `KNOWN_ERROR_MESSAGES` известными сообщениями валидации бэкенда (диапазон оценки, недопустимый статус сущности, ограничения прав team-admin) — приёмка: каждое добавленное сообщение сверено с фактическим текстом в коде бэкенда, а не придумано
- [x] 2.5 Добавить `renderTableState(container, state, options)` для `loading` / `empty` / `error` с кнопкой «Повторить» в состоянии ошибки — приёмка: состояние рендерится в переданный контейнер напрямую, без участия подписок `state.subscribe`
- [x] 2.6 Добавить `openModal(el)` / `closeModal(el)` поверх `<dialog>.showModal()` / `.close()` с явным запоминанием элемента-инициатора для программных открытий — приёмка: `Escape` закрывает модалку, фокус после закрытия возвращается на инициатор

## 3. Frontend — дизайн-система и диаграмма Ганта

Спека: `web-design-tokens`. Правится одним пакетом — см. `design.md`, Decision 1–2.

- [x] 3.1 Применить подобранные в 1.1 значения в `css/variables.css`: шесть ролевых цветов, нейтральные `--color-epic`/`--color-story`, токены `*-progress` для полос прогресса, `--color-danger-bg-subtle`, `--tap-target-icon` — приёмка: `grep` не находит ни одной пары «ролевой ↔ семантический/статусный токен» с одинаковым hex
- [x] 3.2 Заменить в `js/scoring-panel.js` шесть использований `--color-role-be` в значении «успех/итог» (строки ~455, 495, 957, 1133, 1167) на `--color-success` / `--color-success-bg` либо готовые классы `.badge.scored`; убрать хардкод `rgba(16, 185, 129, ...)` — приёмка: в файле не осталось `--color-role-*` для обозначения состояния
- [x] 3.3 Убрать окраску показателей вероятности и влияния риска цветами ролей (`js/scoring-panel.js` ~1010-1011) — приёмка: в блоке оценок риска не используются `--color-role-*`
- [x] 3.4 Согласовать обозначение типа задачи в таблице квот (`js/reports-panel.js` ~401, 412) с бейджами типов в таблице вместимости; категорию «Architecture + Techdebt» оставить текстом без цветовой точки — приёмка: цвет типа в обеих таблицах вкладки «Отчёты» совпадает, у объединённой категории цветового индикатора нет
- [x] 3.5 Заменить хардкод `rgba(239, 68, 68, 0.05)` в `js/reports-panel.js` (~385) на токен из 3.1 — приёмка: в файле нет литеральных rgba-цветов
- [x] 3.6 Русифицировать попап Frappe Gantt в `js/gantt-renderer.js` (~520-522): `Progress:` и `day/days` — приёмка: подсказка при наведении на бар не содержит английских слов
- [ ] 3.7 Проверить визуально диаграмму с эпиками, историями и ролевыми задачами всех шести ролей одновременно — приёмка: роль бара определяется по цвету, уровень — по заливке и вложенности; ни один уровень не сливается с ролевым цветом
- [x] 3.8 Заменить хардкод цветов полосы прогресса в `css/gantt.css` (`#3730a3`, `#7c3aed`, `#1d4ed8`, `#047857`, `#d97706`, `#db2777`, `#4338ca`) на токены `*-progress` из 3.1 — приёмка: в `gantt.css` не осталось литеральных hex-цветов в правилах `.bar-progress`
- [x] 3.9 Реализовать различение уровней заливкой в `css/gantt.css`: `.gantt-epic .bar` — сплошная заливка, `.gantt-story .bar` — полупрозрачная с контуром; сохранить существующее вторичное кодирование (скругление 6px/4px, начертание подписи 700/600) — приёмка: эпик и стори различимы при чёрно-белой печати страницы

## 4. Frontend — подтверждение деструктивных действий

Спека: `web-destructive-confirm`.

- [x] 4.1 Завести `openDeleteStoryModal(story)` по образцу `openDeleteEpicModal` с текстом из 1.3 и заменить `confirm()` в `js/scoring-panel.js` (~1265) — приёмка: удаление истории открывает кастомную модалку с упоминанием удаляемых рисков и оценок
- [x] 4.2 Добавить подтверждение снятия администратора команды в `js/admin-panel.js` (`removeTeamAdmin`, ~723) с указанием пользователя и команды — приёмка: одиночный клик по «Снять» не отправляет запрос на сервер
- [x] 4.3 Заменить `alert()` в `js/scoring-panel.js` (~1540) на уведомление средствами интерфейса — приёмка: `grep -rn 'confirm(\|alert(' web/gantt/js/` не находит ни одного вхождения

## 5. Frontend — обратная связь и состояния экранов

Спека: `web-async-feedback`.

- [x] 5.1 Перевести все восемь форм `js/admin-panel.js` (команда ~287, эпик ~304, история ~379, риск ~469, импорт ~494, пользователь ~511, редактирование пользователя ~546, назначение тим-админа ~602) на `withSubmitLock` — приёмка: быстрый двойной клик по любой из кнопок отправки создаёт ровно одну сущность
- [x] 5.2 Привести существующие ручные блокировки (`js/app.js:runRegenerateQuarter`, `js/scoring-panel.js` создание истории, `js/gantt-renderer.js:saveTaskDetails`) к `withSubmitLock` — приёмка: паттерн блокировки в кодовой базе один
- [x] 5.3 Исправить `loadUsers` в `js/admin-panel.js` (~145-157): рендерить состояние ошибки в `#table-users-body` через `renderTableState` — приёмка: при недоступном `/admin/users` таблица показывает ошибку с кнопкой «Повторить», а не бесконечную «Загрузку…»
- [x] 5.4 Развести состояния «ошибка» и «нет администраторов» в `loadTeamAdmins` / `renderTeamAdminsTable` (`js/admin-panel.js` ~672-689) — приёмка: при сетевой ошибке текст отличается от текста для пустого списка
- [x] 5.5 Перевести все обработчики ошибок в `js/scoring-panel.js`, `js/admin-panel.js`, `js/reports-panel.js` с `showToast('...' + err.message)` на `handleApiError` — приёмка: `grep` не находит конкатенации `err.message` в тексте для пользователя
- [x] 5.6 Добавить `aria-live` / `role` на `#toast-container` в `index.html` (~674), для тостов типа `error` — усиленный вариант объявления — приёмка: появление тоста объявляется программой экранного доступа без перевода фокуса
- [x] 5.7 Добавить действия «Обновить» и «Выйти» на экран «Доступ ограничен» (`index.html` ~51-57, `js/auth.js`) — приёмка: с экрана доступно повторение проверки доступа без ручной перезагрузки браузера
- [x] 5.8 Различить в `checkAuth` (`js/auth.js` ~48-58) отказ в авторизации и сетевую ошибку — приёмка: при недоступном сервере показывается состояние «не удалось подключиться» с повтором, а не экран входа

## 6. Frontend — экранирование пользовательского текста

Спека: `web-content-escaping`. См. `design.md`, Decision 5 — в `ai-chat.js` порядок операций критичен.

- [x] 6.1 Применить `escapeHtml` ко всем подстановкам пользовательского текста в `js/scoring-panel.js` (описания эпика ~467, истории ~1155, риска ~1090; имена участников ~981, ~1006-1009) — приёмка: описание с `<`, `>`, `&` отображается целиком, вёрстка карточки не ломается
- [x] 6.2 Применить `escapeHtml` к пользовательскому тексту в `js/reports-panel.js` — приёмка: названия эпиков с разметочными символами отображаются буквально в таблицах отчёта
- [x] 6.3 Экранировать текст в `appendMessage` (`js/ai-chat.js` ~39-56) до применения markdown-замен — приёмка: вопрос `<b>test</b>` отображается как текст, при этом переносы строк и `**жирный**` продолжают работать
- [x] 6.4 Проверить подстановки в атрибуты (`title`, `data-*`) в затронутых файлах — приёмка: значение с кавычкой не разрывает атрибут

## 7. Frontend — базовая семантика и тач-цели

Спека: `web-ui-foundations`.

- [x] 7.1 Понизить заголовки оверлеев авторизации и отказа в доступе в `index.html` (~40, ~54) до второго уровня, оставив `<h1>` только у названия приложения в шапке (~64) — приёмка: `document.querySelectorAll('h1').length === 1` при любом активном оверлее
- [x] 7.2 Задать `.btn-icon` квадратную область ≥40px на всех брейкпоинтах (`css/components.css` ~835-850) и убрать правило `min-height` без `min-width` из `css/responsive.css` (~273-283) — приёмка: на ширине ≤768px кнопка закрытия остаётся кругом, не превращаясь в овал
- [x] 7.3 Заменить «SP» на «чд» в `js/app.js` (~255) — приёмка: в опциях выбора эпика оценка подписана «чд»

## 8. Frontend — модалки на `<dialog>`

Спека: `web-modal-accessibility`. Выполняется после секций 4 и 7 — см. `design.md`, Migration Plan.

- [x] 8.1 Привести `openEditRiskModal` (`js/scoring-panel.js` ~1824-1842) к структуре `.modal-header` / `.modal-body` / `.modal-footer` по образцу `openEditEpicModal` — приёмка: на ширине ≤480px кнопки футера ведут себя как в остальных модалках
- [x] 8.2 Перевести все 10 существующих модалок из списка 1.2 (плюс две новые из секции 4) на `<dialog>` через `openModal` / `closeModal`, заменив `.modal-overlay` на `::backdrop` с явными значениями фона — приёмка: каждая модалка из списка открывается, закрывается по `Escape` и по клику на фон, вёрстка не изменилась
- [x] 8.3 Добавить программное название модалки, связанное с её заголовком — приёмка: программа экранного доступа объявляет модалку как диалог с её заголовком
- [x] 8.4 Добавить модификатор футера для деструктивных модалок, отменяющий `column-reverse` на ≤480px (`css/responsive.css` ~352-365) — приёмка: в модалках удаления «Отмена» читается выше деструктивной кнопки, в остальных модалках порядок прежний
- [x] 8.5 Проверить перевод фокуса внутрь модалки при открытии и возврат на инициатор при закрытии для каждой модалки из списка 1.2 — приёмка: перебор клавиатурой не выводит фокус на страницу под модалкой

## 9. Frontend — косметика

- [x] 9.1 Привязать селектор `.modal-overlay` в `setupGanttEvents` (`js/gantt-renderer.js` ~1342) к `#reorder-modal` — приёмка: селектор явно скоупнут, как соседний `#task-details-modal .modal-overlay` (снята: 8.2 убрал `.modal-overlay` из проекта полностью, закрытие по фону реализовано слушателем, скоупнутым на `#reorder-modal`)
- [x] 9.2 Удалить мёртвые правила `.btn-vote` / `.vote-options-grid` (`css/components.css` ~571-601, `css/responsive.css` ~319-327) — приёмка: `grep` подтверждает, что эти классы не используются ни в одном JS/HTML-файле

## 10. QA — верификация (субагент `qa`)

- [x] 10.1 Прогнать механически проверяемые критерии: единственный `<h1>`, отсутствие `confirm(`/`alert(`, отсутствие `--color-role-*` в семантических контекстах, отсутствие пар «ролевой = семантический» токен, отсутствие конкатенации `err.message` в пользовательских сообщениях — приёмка: отчёт по каждому пункту с командой проверки
- [ ] 10.2 Проверить сценарии двойной отправки для всех восьми форм админ-панели — приёмка: по каждой форме зафиксировано, что создаётся ровно одна сущность
- [ ] 10.3 Проверить ветки ошибок: недоступный `/admin/users`, недоступный список тим-админов, недоступный `/profile` — приёмка: ни в одном случае экран не остаётся в состоянии загрузки и ошибка не выглядит как пустой результат
- [ ] 10.4 Проверить клавиатурные сценарии модалок из списка 1.2 — приёмка: по каждой модалке зафиксировано открытие, `Escape`, удержание фокуса и возврат фокуса
- [ ] 10.5 Проверить отображение текста с символами `<`, `>`, `&` в описании эпика, истории, риска, имени участника и вопросе к AI-чату — приёмка: во всех пяти местах текст показан буквально

## 11. UX — приёмочное ревью (субагент `ux`)

- [ ] 11.1 Провести повторное ревью затронутых экранов по критериям приёмки спеков `web-design-tokens`, `web-destructive-confirm`, `web-modal-accessibility`, `web-async-feedback`, `web-content-escaping`, `web-ui-foundations` — приёмка: по каждому требованию спеков зафиксировано «выполнено» либо конкретное расхождение
