## 1. Backend: карточка эпика использует RoundedTotal вместо FinalScore

- [x] 1.1 В `config/template.html` заменить `Итого: {{printf "%.0f" .FinalScore}}` (шапка карточки эпика) на `Итого: {{.RoundedTotal}}` — `RoundedTotal` уже `int`, дробное форматирование не нужно
- [x] 1.2 В `config/template.html` заменить `Итоговая оценка с учётом рисков: <strong>{{printf "%.0f" .FinalScore}}</strong>` (низ карточки эпика) на `Итоговая оценка с учётом рисков: <strong>{{.RoundedTotal}}</strong>`
- [x] 1.3 Убедиться, что таблица «Оценки по ролям» и строка «Сумма оценок (без рисков)» (`.RawRoleScores`/`.RawTotalScore`) не затронуты — эта часть карточки не меняется

## 2. QA

- [x] 2.1 В `internal/report/generator_test.go` (`TestTemplateHTML_RendersWithoutError`) обновить fixture/ассерты: там, где раньше проверялось совпадение «Итого»/«Итоговая оценка с учётом рисков» с `FinalScore`, теперь проверить совпадение с `RoundedTotal` — числа должны отличаться от `FinalScore`, если поячеечное округление даёт другую сумму (используй fixture с дробными `RoleScores`, чтобы `RoundedTotal` заведомо не совпадал с `FinalScore` — иначе тест не поймает регрессию)
- [x] 2.2 Явно проверить в тесте, что значение «Итого»/«Итоговая оценка с учётом рисков» в карточке эпика численно равно `RoundedTotal`, который также используется в главной матрице для того же эпика (`RoundedTotal` — то же поле, что заполняет колонку «Итого с рисками (чд)» в матрице) — то есть тест должен ловить именно рассогласование между карточкой и матрицей, а не просто наличие текста
- [x] 2.3 Прогнать `go build ./...`, `go vet ./...`, `go test ./...` — всё зелёное; убедиться, что `TestCapacityReport_XLSXAndPDFHTMLMatchForSamePeriod` по-прежнему проходит (эта задача не меняет данные, которые он сверяет)
