## 1. Frontend: таблица «Оценки участников (Админ)»

- [x] 1.1 В `web/gantt/js/scoring-panel.js` (`renderAdminScoresTableRows`) добавить нормализацию `telegram_id`: обрезать любой ведущий `@` перед формированием отображаемого ника, чтобы результат всегда содержал ровно один `@`; проверить на строке с `Карим Гараев` и другими участниками из скриншота, что вместо `@@kkkkaara` показывается `@kkkkaara`.
- [x] 1.2 Обернуть отображаемый ник в `<a href="https://t.me/<нормализованный username без @>" target="_blank" rel="noopener noreferrer">`, сохранив текущий приглушённый цвет текста и добавив стиль ссылки (курсор-указатель, подчёркивание на hover); проверить, что при пустом `telegram_id` ссылка не рендерится (как и раньше).
- [ ] 1.3 Вручную проверить в браузере на данных из скриншота (эпик CREDCARD-47, вкладка «Скоринг» → «Оценки участников (Админ)»): ник показывается с одним `@`, клик по нику открывает `https://t.me/<username>` в новой вкладке. (Требует запущенного стека с реальными данными и Telegram-логина — недоступно из агентской сессии, выполнена только статическая проверка кода/синтаксиса.)

## 2. QA

- [x] 2.1 Убедиться, что регресс не затронул другие места отображения `telegram_id` (например, `web/gantt/js/admin-panel.js`), где `@`-логика не менялась и не должна была измениться.
