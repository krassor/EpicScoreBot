## 1. Backend

- [x] 1.1 В `internal/transport/httpServer/handlers/reports.go`, структура
      `EpicReportItem` (~строка 26-34): добавить поле
      `RawRoleScores map[string]float64 \`json:"raw_role_scores"\`` —
      с коротким комментарием, что `role_scores` риск-скорректированы, а
      `raw_role_scores` — сырые (до риск-фактора).
- [x] 1.2 В `GetCapacityReport` (~строки 147-212): для ветки с оценёнными
      историями (~155-182) и для fallback-ветки без историй (~185-212)
      параллельно с текущим накоплением риск-скорректированных значений
      в `item.RoleScores` — накапливать сырые (`rs.WeightedAvg` без
      умножения на `storyRiskFactor`/`riskFactor`) в
      `item.RawRoleScores`. `rolePlanned`/`role_capacities[].planned` — не
      трогать, они остаются риск-скорректированными.
- [x] 1.3 Обновить контракт эндпоинта в `.agents/tasks/task_backend.md`
      (описание `GET /reports/capacity` / `GetCapacityReport`) — добавить
      описание нового поля `raw_role_scores`.

## 2. Frontend

- [x] 2.1 В `web/gantt/js/reports-panel.js`, рендеринг per-epic строки
      таблицы (~строки 178-198): колонки по ролям и «Итого (чд)»
      переключить с `epic.role_scores[roleName]` на
      `epic.raw_role_scores[roleName]` (с тем же фоллбэком на `0`, если
      поле/роль отсутствует). «С рисками (чд)» (`epic.final_score`) — без
      изменений.
- [x] 2.2 В подсчёте суммарной строки по всем эпикам (~строки 214-221,
      `totalRawScoreSum`) — суммировать `raw_role_scores` вместо
      `role_scores`. Строки «Запланировано (трудоемкость)»/«Доступно»/
      «Разница» (используют `role_capacities[].planned`) — не менять.
- [x] 2.3 Проверить в браузере (dev-сервер, отчёт «Вместимость и
      распределение эпиков», на команде с хотя бы одним эпиком, где
      риск-фактор ≠ 1, т.е. `final_score` заметно отличается от суммы
      базовых ролевых оценок): «Итого (чд)» и «С рисками (чд)» теперь
      показывают разные числа для такого эпика; для эпика без риска (или
      риск-фактор = 1) оба значения по-прежнему совпадают — это
      корректное совпадение для конкретного случая, а не системная
      ошибка. Строки «Запланировано»/«Доступно»/«Разница» визуально не
      изменились.

## 3. QA

- [x] 3.1 Юнит-тест на `GetCapacityReport` (или на функцию расчёта
      ролевых оценок эпика, если она выделена в рамках 1.2): для эпика с
      `final_score`, отличным от суммы `WeightedAvg` по ролям, —
      `raw_role_scores` не равны `role_scores`, и сумма
      `raw_role_scores` не равна `final_score`; сумма `role_scores`
      по-прежнему равна `final_score` (регресс на существующее
      поведение).
- [x] 3.2 Юнит-тест на случай эпика с оценёнными сторями — сырые ролевые
      оценки агрегируются по историям без применения `storyRiskFactor`
      каждой истории.
