## 1. Frontend

- [x] 1.1 Дождаться реализации `fix-capacity-report-raw-total` (нужно поле
      `raw_role_scores` в ответе `GET /reports/capacity`) — без него
      формулы «Итого (чд)»/«С рисками (чд)» из 1.2 не соответствуют
      фактическому поведению.
- [x] 1.2 В `web/gantt/index.html`, вкладка `tab-content-reports`
      (~строки 416-461): между блоком фильтров и `.reports-grid`
      добавить `<div class="admin-card">` с заголовком «Как считаются
      показатели» и двумя подсписками формул — для таблицы «Вместимость
      и распределение эпиков» (доступная ёмкость роли = `MembersCount *
      8 * 6 * 0.838`; оценка эпика по роли — сырая, без риска; «Итого
      (чд)» — сумма сырых оценок по ролям; «С рисками (чд)» —
      `final_score`; «Запланировано (трудоемкость)» — риск-скорректированная
      плановая нагрузка по роли; «Разница» = «Доступно» − «Запланировано»)
      и для таблицы «Квоты по типам задач» («Целевой лимит» — фиксированный
      порог; «Фактический процент» — доля суммы `final_score` эпиков
      данной группы типов от суммы `final_score` всех оценённых эпиков
      периода). Стилизовать компактно, мелким приглушённым шрифтом, без
      сворачивания — блок должен быть виден сразу.
- [x] 1.3 Проверить в браузере (dev-сервер, вкладка «Отчёты»): блок с
      формулами виден сразу при открытии вкладки без выбора команды и
      без дополнительных кликов; при выборе команды/года/квартала и
      построении отчёта блок не пропадает и не перекрывает таблицы;
      внешний вид не ломает существующую вёрстку вкладки на десктопной и
      суженной ширине окна.
