## 1. Frontend: фикс синхронизации selectedStory

- [x] 1.1 В `web/gantt/js/scoring-panel.js`, в функции `loadEpicData()`, заменить условное присваивание `selectedStory` на безусловный поиск свежего объекта по id в только что загруженном `currentStories` (см. design.md, Decision 1): если сторя с текущим `selectedStory.id` найдена — использовать именно её, иначе (не выбрана / не найдена) — `currentStories[0]`, как и раньше. Проверить: `node --input-type=module --check web/gantt/js/scoring-panel.js` проходит.
- [x] 1.2 Убедиться, что поведение при пустом `currentStories` и при отсутствии `selectedStory` не изменилось (ветка `else` и первоначальный выбор стори остаются как есть) — статический просмотр диффа, изменена только логика выбора внутри `if (currentStories.length > 0)`.

## 2. QA: сквозная проверка

- [x] 2.1 Проверить сценарий из спеки (`specs/epic-scoring-panel/spec.md`): администратор массово проставляет оценку последней недостающей роли через «Оценить всей ролью» → без перезагрузки страницы таблица «Оценки по ролям» сразу показывает реальные агрегированные оценки вместо прочерков, элементы массового голосования пропадают.
- [x] 2.2 Проверить второй сценарий спеки: обычный участник отправляет последний недостающий личный голос → без перезагрузки панель отражает переход стори в `SCORED` (финальная оценка, элементы переопределения для админа).
- [x] 2.3 Проверить, что при действиях, не завершающих скоринг (например, промежуточный личный голос, не последний), выбранная сторя в списке слева не сбрасывается и не переключается на другую — поведение соответствует третьему сценарию спеки.
- [x] 2.4 Прогнать `go test ./...` (изменения фронтенд-only, но полезно подтвердить отсутствие непреднамеренных побочных эффектов в остальном репозитории) — регрессий нет.
