## 1. Backend — данные для диаграммы

- [x] 1.1 Добавить в `internal/report/types.go` поле с задачами Ганта в `ReportData` — рядом с `Risks`/`Generated`, тем же способом, каким PDF уже добавляет то, чего нет в общем ядре агрегации. В `CapacityReportResponse` не добавлять: это общее ядро с JSON `/reports/capacity` и XLSX, его формат менять нельзя (design.md Решение 4). Проверка: `go build ./...` проходит, JSON-ответ `/reports/capacity` не изменился.
- [x] 1.2 Заполнять новое поле в `GetReportData` из существующего метода сервиса Ганта, возвращающего задачи команды. Ошибку получения задач не пробрасывать наружу как отказ отчёта — логировать и оставлять поле пустым (design.md Решение 5). Проверка: юнит-тест — при ошибке источника задач `GetReportData` возвращает данные отчёта без задач и без ошибки.

## 2. Backend — построение SVG диаграммы

- [x] 2.1 Реализовать построитель SVG диаграммы Ганта в `internal/report/` тем же приёмом, что существующие диаграммы рисков (`svg.go`): возвращает строку SVG, встраиваемую в шаблон как `template.HTML`. Дерево строк — эпики, стори, ролевые задачи, порядок как на вкладке «Гант». Проверка: юнит-тест на полном дереве — в выводе присутствуют строки всех трёх уровней в ожидаемом порядке.
- [x] 2.2 Построить календарную шкалу, покрывающую весь диапазон дат задач, включая выходящие за границы квартала отчёта, с подписями, по которым определяется период любого бара. Проверка: тест — задача, заканчивающаяся за пределами квартала, отрисована целиком, границы шкалы охватывают её дату окончания.
- [x] 2.3 Отрисовать бары по плановым датам задач и подписи строк с ролью и исполнителем ролевой задачи. Проверка: тест — координаты бара соответствуют датам задачи относительно границ шкалы; в подписи ролевой задачи с исполнителем присутствуют и роль, и исполнитель.
- [x] 2.4 Реализовать кодирование состояний по design.md Решение 6: роль, завершённость, отсутствие исполнителя — признаком, сохраняющимся при чёрно-белой печати, а не только заливкой; плюс легенда на листе. Проверка: тест — задачи разных ролей, завершённая задача и задача без исполнителя дают различающийся вывод помимо атрибута цвета; легенда присутствует и покрывает применённые обозначения.
- [x] 2.5 Вычислять высоту листа и высоту SVG из одного числа строк по одной формуле в одном месте (design.md Решение 2): отступы + шапка со шкалой + число строк × фиксированная читаемая высота строки. Ширина постоянная, соответствует A3 в альбомной ориентации. Проверка: тест на нескольких размерах набора задач (пусто, один эпик, полное дерево, несколько десятков задач) — высота листа и высота SVG согласованы, ширина одинакова во всех случаях.
- [x] 2.6 Ограничить состав диаграммы отчётным периодом: оставлять только задачи эпиков отчётного года/квартала. `GetTeamTasks(teamID)` (`ganttService.go:1098`) фильтра по периоду не имеет и возвращает все задачи команды, поэтому лист отчёта за Q4 показал бы заодно задачи Q1–Q3 — это расходится с требованием спеки «диаграмма запланированных работ этой команды за этот же период». Фильтровать по эпикам периода через существующий `GetEpicsByTeamYearQuarter` (`repositories/epic.go:372`). Не путать с требованием «Календарная шкала покрывает весь диапазон работ»: задача эпика отчётного квартала, заканчивающаяся уже в следующем, по-прежнему показывается целиком и не обрезается по границе квартала. Добавленный объём сверх исходного плана — согласован с пользователем. Проверка: тест — у команды есть эпики двух кварталов, в диаграмме отчёта присутствуют только строки эпиков отчётного, а задача отчётного эпика с окончанием за границей квартала не обрезана.

## 3. Backend — сборка PDF

- [x] 3.1 Вынести разметку листа диаграммы в собственный шаблон, не трогая существующий `config/template.html` (design.md, Risks: так откат не потребует восстановления внешнего шаблона). Проверка: существующие разделы отчёта рендерятся байт в байт как раньше на тех же входных данных.
- [x] 3.2 Реализовать в `internal/report/generator.go` вторую конвертацию — лист диаграммы через `PaperSize(ширина, высота)` вычисленными в 2.5 величинами — и объединение с основным отчётом через `PDFEngines().Merge()` (design.md Решение 1). Основная часть конвертируется как сегодня, `PaperSizeA4()`. Проверка: на выходе один файл, в нём после разделов отчёта идёт лист диаграммы заявленного размера.
- [x] 3.3 Пересмотреть тайм-ауты под три обращения к Gotenberg вместо одного (сейчас 60 секунд и на HTTP-клиенте, и на контексте запроса). Проверка: генерация отчёта с полным деревом задач укладывается в тайм-аут с запасом; при исчерпании тайм-аута возвращается внятная ошибка, а не зависание.
- [x] 3.4 Реализовать деградацию по design.md Решение 5: отсутствие задач и ошибка построения диаграммы дают отчёт без листа диаграммы (либо с явным сообщением об отсутствии запланированных работ) и запись в журнал, а не отказ выгрузки. Пустая размеченная сетка без объяснения не выводится. Проверка: тесты — команда без задач и смоделированный сбой построения дают успешную выгрузку с остальными разделами.

## 4. QA

- [x] 4.1 Покрыть тестами построение SVG на граничных наборах: пустой, один эпик без сторей (legacy), полное дерево, задачи без исполнителя, задачи с датами за пределами квартала. Проверка: тесты проходят, каждый набор даёт ожидаемый состав вывода.
- [x] 4.2 Проверить, что XLSX-выгрузка и JSON `/reports/capacity` не изменились относительно текущего поведения. Проверка: существующие тесты `internal/report/` (включая `cross_format_test.go`) проходят без изменения ожиданий.
- [x] 4.3 Прогнать `go test ./...` и покрытие `go tool cover` по `internal/report` — убедиться, что покрытие не ниже прежнего уровня. Проверка: тесты зелёные, покрытие не просело.

## 5. Ручная проверка

Автотесты проверяют состав SVG и устойчивость конвейера, но не то, как лист выглядит и печатается — это проверяется руками на сгенерированном файле.

- [x] 5.1 Выгрузить PDF-отчёт команды с полным деревом задач и проверить сценарии спеки по составу и порядку: все три уровня присутствуют, порядок совпадает с вкладкой «Гант» за тот же период, даты баров соответствуют вебу. Проверка: расхождений нет.
- [x] 5.2 Проверить размер листа на командах с сильно разным числом задач. Проверка: ширина одинакова, высота различается, все строки на одном листе, высота строки читаемая.
- [x] 5.3 Распечатать лист (или сохранить в чёрно-белом) и проверить кодирование состояний. Проверка: роли, завершённые задачи и задачи без исполнителя различимы без цвета, легенда покрывает обозначения.
- [x] 5.4 Выгрузить отчёт команды, для которой задачи Ганта не генерировались. Проверка: выгрузка успешна, остальные разделы на месте, пустой размеченной сетки нет.
