## 1. Backend — схема БД и слой данных

- [x] 1.1 Создать миграцию `internal/migrator/migrations/011_task_assignees.sql`: таблица `task_assignments (epic_id, role_id, user_id NULL, start_offset_days)` с PK `(epic_id, role_id)`, `ON DELETE CASCADE` на `epics`/`roles` и `ON DELETE SET NULL` на `users`; колонка `gantt_tasks.assignee_id UUID NULL REFERENCES users (id) ON DELETE SET NULL`; перенос ненулевых `gantt_tasks.start_offset_days` запросом из design.md Решение 5. Колонку `gantt_tasks.start_offset_days` **не удалять**. Проверка: миграция применяется на чистой БД и на копии существующей без ошибок, `SELECT` из `task_assignments` показывает перенесённые ненулевые смещения.
- [x] 1.2 Добавить в `internal/models/domain/gantt.go` поле `AssigneeID *uuid.UUID` в `GanttTask` и доменный тип пользовательского ввода задачи (закрепление + смещение) с комментарием, что `StartOffsetDays` теперь приходит из `task_assignments`, а не из строки Ганта. Проверка: `go build ./...` проходит.
- [x] 1.3 Реализовать в `internal/repositories/gantt.go` доступ к `task_assignments`: чтение всех записей команды одним запросом, upsert закрепления, сброс закрепления (`user_id = NULL`), upsert смещения старта; и запись `gantt_tasks.assignee_id`. Проверка: юнит-тесты репозитория на upsert/сброс/чтение пачкой проходят.
- [x] 1.4 Расширить контракт `internal/gantt/interfaces.go` новыми методами репозитория и уже существующим `GetUsersByTeamIDAndRoleID`; обновить `internal/gantt/fakerepo_test.go`. Проверка: `go build ./...` и существующие тесты пакета `gantt` компилируются.

## 2. Backend — планировщик

- [x] 2.1 Загружать в начале `RecalculateTeamSchedule` пулы кандидатов по ролям (`GetUsersByTeamIDAndRoleID`, отсортированы по `uuid`) и закрепления команды — **пачкой, вне цикла по задачам**; завести `assigneeFreeAt` и `rrCursor`, пересоздаваемые на каждый вызов, сохранив `roleFreeAt` для ролей с пустым пулом. Проверка: существующие тесты `internal/gantt/` проходят без изменения ожиданий.
- [x] 2.2 Реализовать выбор исполнителя в `recalculateEpicSchedule` по развилке из design.md Решение 1 (заморожена → пул пуст → закрепление → автомат) и жадный выбор `argmin start(u)` с round-robin тай-брейком по Решению 2; записывать выбранного исполнителя в `gantt_tasks.assignee_id`. Проверка: новый тест — две стори с задачей одной роли и два кандидата дают пересекающиеся по датам задачи у разных людей и меньший суммарный срок, чем при одном кандидате.
- [x] 2.3 Брать `StartOffsetDays` из `task_assignments`, а не из строки `gantt_tasks`. Проверка: существующие тесты смещения старта (`internal/gantt/order_test.go` и соседние) проходят после перевода их фикстур на новый источник.
- [x] 2.4 Не переназначать исполнителя у замороженных задач (`Progress > 0` или `ActualEndDate != nil`), учитывая их эффективное окончание в `assigneeFreeAt` исполнителя. Проверка: тест — задача с ненулевым прогрессом сохраняет исполнителя после пересчёта с изменённым составом команды, а следующая задача той же роли у того же человека начинается после её окончания.
- [x] 2.5 Обрабатывать недействующее закрепление (закреплённый не входит в пул роли): падать в автоматическое распределение, строку `task_assignments` не удалять, отдавать признак недействительности наружу. Проверка: тест — снятие роли у закреплённого переводит задачу на автомат, возврат роли возвращает закрепление в силу.
- [x] 2.6 Перестать терять пользовательский ввод при перегенерации: убедиться, что `generateTaskRowsForEpic` не трогает `task_assignments`, и что после `DeleteGanttTasksByEpicID` + пересоздания закрепления и смещения применяются заново. Проверка: тест — закрепление и ненулевое смещение сохраняются после `GenerateTasksForQuarter`.
- [x] 2.7 Добавить методы сервиса для установки/снятия закрепления исполнителя (с последующим пересчётом расписания команды) и для получения состава команды с ролями. Проверка: юнит-тесты сервиса на оба сценария проходят.

## 3. Backend — HTTP API

- [x] 3.1 Добавить в `ganttTaskResp` (`internal/transport/httpServer/handlers/gantt.go`) поля `assignee_id`, `assignee_name`, `assignee_is_manual` и признак недействующего закрепления. Проверка: тест обработчика `GetTasks` показывает новые поля для задачи с исполнителем и их отсутствие/пустоту для задачи без него.
- [x] 3.2 Реализовать обработчик `PUT /api/gantt/tasks/{id}/assignee` с телом `{ "user_id": "…" | null }`, где `null` — возврат к автоматическому распределению; отклонять запрос для роли `member` и при кандидате, не входящем в пул роли; ошибки — в стандартном формате `{ "error": { "code": "...", "message": "..." } }`. Проверка: тесты обработчика на закрепление, снятие, отказ для `member` и отказ для чужого кандидата.
- [x] 3.3 Реализовать обработчик `GET /api/gantt/teams/{id}/members`, возвращающий участников с **полным** перечнем ролей каждого (не через `GetRoleByUserID`). Проверка: тест — участник с двумя ролями возвращается с обеими; несуществующая команда даёт ошибку в стандартном формате.
- [x] 3.4 Зарегистрировать оба маршрута в `internal/transport/httpServer/routers/routers.go` внутри существующей аутентифицированной группы и добавить новые методы в `handlers/interfaces.go`. Проверка: `routers_mount_test.go` подтверждает, что маршруты смонтированы и закрыты middleware авторизации.
- [x] 3.5 Отдавать в `ganttTaskResp` актуальное `start_offset_days` из `task_assignments`, а не из строки `gantt_tasks` (колонка перестала писаться после задачи 2.3, из-за чего выставленное смещение применяется к расписанию, но возвращается в API нулём). Добавленный объём сверх исходного плана — согласован с пользователем. Проверка: тест обработчика `GetTasks` показывает ненулевое смещение, выставленное через `PUT /api/gantt/tasks/{id}`, в ответе после пересчёта.
- [x] 3.6 Добавить в `SetTaskAssignee` team-scoped проверку админа: после резолва эпика задачи проверять `IsTeamAdminOf(session.TelegramID, epic.TeamID)` тем же приёмом, что уже применяют `admin.go:372` и `admin_scores.go:81` (superadmin проверку проходит). Сейчас эндпоинт закрыт только грубым `RoleAuth("admin")`, поэтому админ одной команды может менять исполнителя в чужой. Добавленный объём сверх исходного плана — согласован с пользователем. Проверка: тест обработчика — админ команды A получает `403` на задачу команды B, админ своей команды и superadmin проходят.

## 4. UX — постановка для фронтенда

- [x] 4.1 Провести UX-ревью и сформировать постановку (субагент `ux`, код не пишет): подача исполнителя в подписи задачи с учётом длины имён и режимов День/Неделя/Месяц; состояния выбора исполнителя в модалке деталей (автоматически / закреплён / закрепление недействительно / кандидатов нет); визуальное отличие задач без исполнителя; как сообщить пользователю, что закрепление гарантирует человека, но может сдвинуть дату. Проверка: постановка зафиксирована текстом с перечнем состояний и правил отображения, согласована с существующей дизайн-системой `web/gantt/css/`. Постановка — `ux-brief.md` в каталоге заявки.

## 5. Frontend

- [x] 5.1 Добавить в модалку деталей задачи (`web/gantt/index.html`) выбор исполнителя с пунктом «Автоматически» и загрузку кандидатов роли из `GET /api/gantt/teams/{id}/members`; вызов сохранения — в `web/gantt/js/api.js`. Проверка: в интерфейсе выбор исполнителя меняет назначение, диаграмма перерисовывается пересчитанными датами.
- [x] 5.2 Выводить исполнителя в подписи задачи в `web/gantt/js/gantt-renderer.js` по постановке из 4.1, сохраняя работу инкрементального `refresh()` и режимов просмотра. Проверка: две пересекающиеся по датам задачи одной роли различимы по подписям во всех трёх режимах просмотра.
- [x] 5.3 Реализовать индикацию задач без исполнителя и недействующих закреплений (`web/gantt/js/gantt-renderer.js`, `web/gantt/css/`). Проверка: задача роли без кандидатов и задача с закреплением на выбывшего человека визуально отличимы от обычных.
- [x] 5.4 Скрывать управление исполнителем для роли `member` тем же признаком, что и остальное редактирование. Проверка: под пользователем с ролью `member` выбор исполнителя недоступен.
- [x] 5.5 Переопределить попап Frappe Gantt (опция `popup`; в исходной формулировке значилось `custom_popup_html` — имя из версии 0.x, в 1.2.2 такой опции нет): при наведении/тапе на ролевой бар попап показывает строку состояния исполнителя («Назначен автоматически» / «Закреплён вручную» / «⚠ Закрепление недействительно — работает автоматически» / «Нет исполнителя — нет участников с этой ролью в команде»). Это единственный канал такой информации для роли `member`, которой модалка недоступна. Добавленный объём сверх исходного плана — согласован с пользователем. Проверка: для родительских (эпик/стори) баров попап выглядит как сегодня, без регрессии — дефолтную разметку Frappe Gantt 1.2.2 сверить фактически, а не по памяти; для ролевых баров строка состояния соответствует данным задачи. **Примечание:** в установленной версии 1.2.2 опция называется `popup`, а не `custom_popup_html` (проверено по исходникам UMD-бандла с CDN) — реализовано под фактическим именем опции, см. отчёт по задаче.
- [x] 5.6 Добавить на вкладку «Гант» компактную легенду по всем состояниям бара, а не только по двум новым: цвета ролей, завершённость, факт-маркер, нет исполнителя, недействующее закрепление. Сейчас легенды нет вообще, а эта заявка доводит число несловесных состояний до пяти. Добавленный объём сверх исходного плана — согласован с пользователем. Проверка: легенда покрывает все состояния, использует те же токены и классы, что и сама диаграмма, и не ломает вёрстку тулбара на узких экранах.

## 6. QA

- [x] 6.1 Тест детерминизма: два последовательных `RecalculateTeamSchedule` без изменения входных данных дают идентичное распределение исполнителей (защита от рандомизации итерации `map`). Проверка: тест проходит стабильно при многократном прогоне (`go test -count=20 ./internal/gantt/`).
- [x] 6.2 Тест round-robin тай-брейка: при нескольких свободных кандидатах и одинаковой получаемой дате старта задачи распределяются по кругу, а не достаются одному. Проверка: тест проходит.
- [x] 6.3 Тест приоритета закрепления: закрепление на занятого человека назначает задачу ему и сдвигает её старт, зависящие задачи пересчитываются. Проверка: тест проходит.
- [x] 6.4 Тест роли без кандидатов: генерация не падает, задачи без исполнителя выстроены последовательно (прежнее поведение). Проверка: тест проходит.
- [x] 6.5 Тест устойчивости к перегенерации: закрепления и смещения переживают `GenerateTasksForQuarter` и переоценку стори. Проверка: тест проходит.
- [x] 6.6 Прогнать полный набор тестов и покрытие: `go test ./...` и `go tool cover` — убедиться, что пакеты `internal/gantt` и `internal/transport/httpServer/handlers` не потеряли покрытие относительно текущего уровня. Проверка: тесты зелёные, покрытие не ниже прежнего.
