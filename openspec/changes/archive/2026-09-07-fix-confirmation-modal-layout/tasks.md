## 1. Frontend: привести модалки подтверждения к стандартной вёрстке

- [x] 1.1 В `web/gantt/js/scoring-panel.js` переписать `openExpertRoleScoreModal` (~строки 761-822) на структуру `.modal-content > .modal-header (h2 + .btn-icon.btn-close-modal) + .modal-body (текст + предупреждение) + .modal-footer (кнопки)`, как в эталонной `openEditEpicModal` — убрать все инлайн-стили `style="..."` из этого шаблона, оставить только текст/классы кнопок/обработчики без изменений. Проверить: `node --input-type=module --check web/gantt/js/scoring-panel.js` проходит.
- [x] 1.2 Аналогично переписать `openDeleteEpicModal` (~строки 1779-1809) на ту же стандартную структуру. Проверить: `node --input-type=module --check web/gantt/js/scoring-panel.js` проходит.
- [ ] 1.3 Ручная проверка в браузере: обе модалки (удаление эпика, экспертная оценка роли) визуально выглядят как остальные модалки приложения (паддинги, разделители заголовка/футера, размер шрифта заголовка) — текст и функциональность (кнопки, закрытие, сами действия удаления/проставления оценки) не изменились.
