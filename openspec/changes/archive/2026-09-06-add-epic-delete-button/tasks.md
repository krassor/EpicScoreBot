## 1. Backend: маршрут удаления эпика

- [x] 1.1 В `internal/transport/httpServer/handlers/admin.go` заменить в `DeleteEpic` `chi.URLParam(r, "id")` на `chi.URLParam(r, "epic_id")`; проверить `go build ./...` проходит без ошибок
- [x] 1.2 В `internal/transport/httpServer/routers/routers.go` зарегистрировать `mux.Delete("/epics/{epic_id}", r.ganttHandler.DeleteEpic)` в существующей `superadmin`-группе (там же, где `/admin/team-admins`); проверить, что приложение стартует без паники chi на конфликте параметров маршрута
- [x] 1.3 Написать/дополнить unit-тесты хендлера (по аналогии с `admin_test.go`/`stories_test.go`): успешное удаление существующего эпика (`200 {"status":"ok"}`), 400 на невалидный `epic_id`, 500 при ошибке репозитория — `go test ./internal/transport/httpServer/handlers/...` проходит
- [x] 1.4 Написать/дополнить тест доступа (по аналогии с `admin_team_scope_test.go`): роль `superadmin` получает `200`, роли `admin`/`leader`/`member` — `403 {"error":{"code":...}}` в стандартном формате ошибок проекта

## 2. Frontend: кнопка удаления и модалка подтверждения

- [x] 2.1 В `web/gantt/js/scoring-panel.js` (`renderDetails()`) добавить флаг `isSuperAdmin` и кнопку «🗑️ Удалить» рядом с кнопкой «✏️ Редактировать» в заголовке панели деталей эпика, видимую только при `isSuperAdmin`; проверить вручную в браузере под ролью superadmin и под ролью admin (кнопка не отображается)
- [x] 2.2 Добавить функцию `openDeleteEpicModal(epic)` по паттерну существующих модалок (`openEditEpicModal`/`openEditRiskModal`): модалка с текстом предупреждения о безвозвратном каскадном удалении эпика вместе с историями/рисками/оценками, кнопками «Отмена» и «Удалить»; проверить, что «Отмена»/клик по оверлею закрывают модалку без запроса к API
- [x] 2.3 По подтверждению в модалке вызвать `apiDelete(`/epics/${epic.id}`)`; при успехе — `showToast` об успехе, сбросить `selectedEpic`/панель деталей через `clearDetails()` и перезагрузить список эпиков через `loadScoringEpics(teamId)`; при ошибке — `showToast`/`showErrorModal` с сообщением, список эпиков не менять
- [x] 2.4 Навесить обработчик клика на новую кнопку удаления в блоке привязки обработчиков `renderDetails()`/`attachDetailsListeners` (там же, где уже навешан `btn-edit-epic`), открывающий `openDeleteEpicModal(selectedEpic)`
- [x] 2.5 Ручная проверка в браузере: под ролью superadmin удалить тестовый эпик в статусе `NEW` и отдельно эпик в статусе `SCORING`/`SCORED` — оба удаляются, список и панель деталей обновляются корректно

## 3. QA: сквозная проверка

- [x] 3.1 Прогнать полный набор бэкенд-тестов (`go test ./...`) и убедиться, что регистрация нового маршрута не сломала существующие тесты роутинга/хендлеров
- [x] 3.2 Проверить сценарии из `specs/epic-scoring-panel/spec.md` вручную или через существующие фронтенд-тесты (если есть): видимость кнопки по ролям, текст предупреждения в модалке, отмена не удаляет эпик, обновление UI после успеха и после ошибки
