## 1. Backend: сериализация пересчёта расписания команды

- [x] 1.1 В `internal/gantt/ganttService.go` добавить в `Service` поле для per-team блокировки (например, `scheduleMu sync.Map` — `teamID uuid.UUID` → `*sync.Mutex`) и helper-метод получения мьютекса конкретной команды (lazy-init через `LoadOrStore`)
- [x] 1.2 В начале `RecalculateTeamSchedule` захватить мьютекс команды, освободить через `defer` — вся функция (все чтения и записи внутри неё) должна выполняться под блокировкой, см. design.md Decision 2
- [x] 1.3 Добавить логирование: старт/финиш `RecalculateTeamSchedule` с `teamID`; отдельно залогировать факт ожидания блокировки (например, через неблокирующий `TryLock()` перед блокирующим `Lock()` — если `TryLock()` не удался, залогировать "ожидание пересчёта расписания команды" и затем сделать блокирующий `Lock()`), см. design.md Decision 4
- [x] 1.4 `grep -rn "RecalculateTeamSchedule" internal/` — убедиться, что найдены все вызывающие места (как минимум `ReorderEpic`, `ReorderStory`, вероятно другие — генерация задач, обновление прогресса и т.п.); подтвердить, что блокировка внутри самой функции защищает их все без отдельных правок на местах вызова
- [x] 1.5 Прогнать `go build ./...`, `go vet ./...`, `go test ./...` — существующие тесты `internal/gantt` (включая `fakerepo_test.go`, `ganttService_test.go`, `order_test.go`) не должны сломаться от добавленной блокировки (mutex безопасен для однопоточных тестовых вызовов)

## 2. QA

- [x] 2.1 Написать тест, имитирующий конкурентный вызов `RecalculateTeamSchedule` для одной команды (две горутины, `sync.WaitGroup`) поверх `fakeRepo` (или его аналога, использованного в расследовании) — убедиться, что блокировка действительно сериализует выполнение (например, через контролируемую задержку внутри mock-реализации одного из методов репозитория, чтобы гарантированно создать окно конкуренции) и что оба вызова не проваливаются с ошибкой/паникой
- [x] 2.2 Проверить, что пересчёты для РАЗНЫХ teamID не блокируют друг друга (не увеличивают друг для друга задержку) — тест или явная проверка в том же тесте из 2.1
- [x] 2.3 Проверено вручную пользователем в браузере на проде — расписание консистентно после пересохранения, гипотеза о гонке подтверждена фактическим устранением проблемы
- [ ] 2.4 Не выполнено — нет доступа к прод-логам в этой сессии; функциональный результат (2.3, расписание больше не ломается) косвенно подтверждает фикс, но записи об ожидании блокировки в логах не проверялись
