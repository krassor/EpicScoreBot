## 1. Frontend

- [x] 1.1 В `web/gantt/js/scoring-panel.js`, блок `adminRiskSelectorsHtml`
      (~строки 528-556), добавить `min-width: 0` в inline-стиль трёх
      `<select class="select ...">` (`risk-admin-user`, `risk-prob`,
      `risk-imp`) — по образцу уже исправленного блока
      `userRiskSelectorsHtml` (строки 504, 513). Убедиться, что явные
      `width: 100%`/`flex: ...` из родительских `.risk-sel-group` теперь
      реально сжимают поля.
- [x] 1.2 Проверить в браузере (dev-сервер, страница скоринга, риск
      истории/эпика в статусе SCORING под сессией admin/superadmin):
      поля "Участник", "Вероятность", "Влияние" и кнопка
      "Оценить за участника" не наезжают друг на друга — как на
      стандартной ширине десктопного окна, так и при сужении окна до
      появления `flex-wrap` (несколько строк вместо одной).
- [x] 1.3 Убедиться, что соседний блок для обычного участника
      (`userRiskSelectorsHtml`) и остальная вёрстка страницы скоринга не
      затронуты (визуальная регрессия отсутствует).
