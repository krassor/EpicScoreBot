## 1. Backend (субагент `backend`)

- [x] 1.1 В `internal/gantt/ganttService.go` вынести тело `GenerateTasksForEpic` (создание parent-задачи эпика, задач сторей, ролевых задач, присвоение `sort_order`) во внутреннюю функцию **без** финального вызова `RecalculateTeamSchedule`; `GenerateTasksForEpic` оставить публичной обёрткой с прежним поведением и сигнатурой. Проверка: существующие тесты `go test ./internal/gantt/...` проходят без изменений в них.
- [x] 1.2 В `internal/gantt/ganttService.go` добавить `GenerateTasksForQuarter(ctx, teamID uuid.UUID, year, quarter int, startDate time.Time)` с типом результата, содержащим счётчики (всего отобрано / перегенерировано / неуспешно / итоговое число задач): отбор эпиков через `GetTeamEpicsOrdered` с фильтрацией в памяти по `Year`, `Quarter` и `Status == domain.StatusScored` с сохранением порядка очереди, генерация по каждому через функцию из 1.1, один `RecalculateTeamSchedule` в конце (design.md Decision 2). Проверка: новый юнит-тест на моках репозитория подтверждает, что `RecalculateTeamSchedule` вызван ровно один раз для квартала из нескольких эпиков.
- [x] 1.3 В `GenerateTasksForQuarter` реализовать поведение частичного успеха: ошибка генерации отдельного эпика логируется и увеличивает счётчик неуспешных, цикл продолжается; при нуле отобранных эпиков задачи не изменяются и пересчёт не запускается (design.md Decision 3, спека — «В массовую перегенерацию попадают только заскоренные эпики»). Проверка: юнит-тесты на сценарии «один эпик падает — остальные перегенерированы» и «нет подходящих эпиков — пересчёт не вызван».
- [x] 1.4 В `internal/transport/httpServer/handlers/interfaces.go` добавить `GenerateTasksForQuarter` в интерфейс gantt-сервиса. Проверка: `go build ./...` проходит.
- [x] 1.5 В `internal/transport/httpServer/handlers/gantt.go` добавить обработчик `GenerateQuarterTasks`: разбор тела `{ team_id, year, quarter, start_date }`, валидация (валидный UUID команды, квартал 1–4, год в допустимом диапазоне, дата `YYYY-MM-DD`), ответ `{ message, epics_total, epics_regenerated, epics_failed, tasks_count }`, ошибки — в стандартном формате проекта `{ "error": { "code": "...", "message": "..." } }`. Проверка: handler-тесты на успешный запрос и на каждый случай некорректной валидации (400, задачи не изменяются).
- [x] 1.6 В `internal/transport/httpServer/routers/routers.go` зарегистрировать `mux.Post("/tasks/generate-quarter", r.ganttHandler.GenerateQuarterTasks)` в той же аутентифицированной группе, где находится `/tasks/generate` (design.md Decision 1). Проверка: запрос без аутентификации отклоняется, с аутентификацией — доходит до обработчика (тест роутинга или ручная проверка через curl с валидным токеном).

## 2. Frontend (субагент `frontend`)

- [x] 2.1 В `web/gantt/index.html` перестроить `.toolbar-left` вкладки Ганта в два ряда: 1-й — `#gantt-year` (select), `#gantt-quarter` (select Q1–Q4), существующее `#start-date`; 2-й — существующие `#epic-select`, `#btn-generate`, новая кнопка `#btn-regenerate-quarter`, существующие `#btn-reorder-epics`, `#btn-reorder-stories`. `.toolbar-right` не менять. Проверка: визуально на вкладке «Гант» видны оба ряда, все существующие элементы управления на месте и работают как раньше.
- [x] 2.2 В `web/gantt/css/components.css` (и `responsive.css` при необходимости) добавить стили двухрядной панели инструментов (design.md Decision 6). Проверка: на узком экране ряды не ломают вёрстку и не вызывают горизонтальный скролл.
- [x] 2.3 В `web/gantt/js/app.js` реализовать заполнение селектов года и квартала: кварталы — фиксированный список Q1–Q4, годы — динамически из `state.get('epics')` плюс всегда текущий год, без дубликатов; значения по умолчанию — текущие календарные год и квартал, дата — сегодня (спека — «Выбор года и квартала на панели инструментов Ганта»). Проверка: при открытии вкладки и выборе команды предзаполнены текущие год/квартал/дата; у команды без эпиков список годов содержит текущий год.
- [x] 2.4 В `web/gantt/js/app.js` фильтровать `#epic-select` по выбранным году и кварталу из уже загруженного списка эпиков (design.md Decision 4): перестроение списка при смене года/квартала/команды, сброс выбора и блокировка `#btn-generate`, если ранее выбранный эпик не попадает в период. Проверка: сценарии спеки «Смена квартала перестраивает список», «Выбранный эпик выпал из периода», «В периоде нет эпиков»; диаграмма Ганта при смене периода не меняется.
- [x] 2.5 В `web/gantt/js/app.js` добавить модалку подтверждения массовой перегенерации по образцу `openDeleteEpicModal` (`web/gantt/js/scoring-panel.js:1878`), с указанием выбранных года, квартала и даты и предупреждением, что перегенерируются только заскоренные эпики периода, а прогресс/фактические даты их задач будут потеряны (design.md Decision 5). Проверка: отмена в модалке не отправляет запрос, подтверждение — отправляет.
- [x] 2.6 В `web/gantt/js/app.js` реализовать вызов `POST /tasks/generate-quarter` (`team_id`, `year`, `quarter`, `start_date`) с блокировкой кнопки на время запроса, перезагрузкой задач после успеха и уведомлением-сводкой (количество перегенерированных, а при наличии — неуспешных эпиков); клиентская валидация «не указана дата» / «не выбрана команда» до отправки. Проверка: сценарии спеки «Пользователь подтверждает операцию», «Пользователь видит итог операции», «Не заполнена дата в интерфейсе».
- [x] 2.7 В `web/gantt/js/app.js` скрывать `#btn-regenerate-quarter` для роли `member` там же, где уже скрывается `#btn-generate`. Проверка: под пользователем с ролью `member` кнопка массовой перегенерации не отображается.

## 3. QA (субагент `qa`)

- [x] 3.1 Прогнать `go build ./...` и `go test ./...`, убедиться, что существующие тесты Ганта и обработчиков не сломаны изменением 1.1. Проверка: сборка и весь набор тестов зелёные.
- [x] 3.2 Проверить покрытие новых путей (`GenerateTasksForQuarter`, `GenerateQuarterTasks`) по сценариям спеки: только `SCORED`-эпики попадают в операцию, эпики других периодов не затронуты, единственный вызов пересчёта расписания, частичный успех, все случаи валидации. Проверка: `go test ./internal/gantt/... ./internal/transport/httpServer/handlers/...` с отчётом покрытия по новым функциям.
- [x] 3.3 Ручная сквозная проверка на вкладке «Гант»: выбор года/квартала фильтрует список эпиков, одиночная генерация работает как раньше, массовая перегенерация квартала от указанной даты не начинает ни один эпик раньше этой даты и сохраняет порядок эпиков в очереди. Проверка: наблюдаемое поведение на диаграмме соответствует сценариям спеки.
